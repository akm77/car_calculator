# Core API
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=info
PUBLIC_BASE_URL=http://localhost:8000

# Live CBR rates (prod only)
ENABLE_LIVE_CBR=false
CBR_CACHE_TTL_SECONDS=1800
CBR_URL=https://www.cbr.ru/scripts/XML_daily.asp

# Rate limits and country filter
RATE_LIMIT_PER_MINUTE=60
AVAILABLE_COUNTRIES=

# Telegram bot (optional)
BOT_TOKEN=

# Environment switch (affects which .env* file is used by settings loader)
ENVIRONMENT=dev

# Domain (used by docker-compose for Nginx)
# DOMAIN=your.domain.com

