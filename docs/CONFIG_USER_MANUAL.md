# Руководство пользователя: управление конфигами через Telegram (без программирования)

## Вступление

Это руководство объясняет, как **обновлять конфигурационные файлы** проекта через **Telegram-бота** — без доступа к серверу, без SSH и **без перезапуска**.

Конфиги влияют на расчёты (тарифы, комиссии, курсы валют и т.д.). Процесс безопасный: бот проверяет файл перед сохранением и делает резервную копию.

Кому подходит: администраторам/менеджерам, которые **не являются программистами**, но отвечают за актуальные ставки и правила.

---

## Что можно менять

Бот поддерживает 4 файла конфигурации:

1. **`fees.yml`** — тарифы по странам и фрахт (стоимость логистики/оформления)
2. **`commissions.yml`** — комиссии компании и банка
3. **`rates.yml`** — курсы валют и утилизационный сбор
4. **`duties.yml`** — таблицы пошлин

---

## Что нужно заранее

1. **Доступ администратора**
   - Ваш Telegram User ID должен быть добавлен в список `ADMIN_USER_IDS` (это делает технический администратор).
2. **Запущенный Telegram-бот**
3. Любой редактор текста для правок (например, Notes/Блокнот/VS Code) и базовое понимание, что такое YAML.

---

## Быстрый старт (3 шага)

### 1) Узнать свой Telegram User ID

Отправьте боту команду:

- `/whoami`

Бот ответит карточкой профиля, где будет **User ID**. Передайте этот ID администратору, чтобы вам выдали доступ.

### 2) Проверить доступ и список конфигов

Отправьте:

- `/list_configs`

Бот покажет список файлов и отметки ✅/❌ (есть файл или нет/доступен или нет).

Если увидите «Access Denied / 🚫 Access Denied» — доступа пока нет.

### 3) Обновлять конфиги по стандартному сценарию

Всегда используйте одну и ту же схему:

1. Скачать текущий файл
2. Отредактировать локально
3. Загрузить обратно через бота
4. Применить изменения командой перезагрузки

---

## Скачивание конфигов

Чтобы скачать файл на устройство, используйте команды:

- `/get_fees` — скачать `fees.yml`
- `/get_commissions` — скачать `commissions.yml`
- `/get_rates` — скачать `rates.yml`
- `/get_duties` — скачать `duties.yml`

Бот пришлёт файл документом. Обычно в подписи будет описание и размер.

---

## Загрузка (обновление) конфигов

### Важно перед началом

- **Имя файла должно быть строго таким же**, как ожидает бот (например, именно `fees.yml`).
  - Нельзя загружать `fees_new.yml`, `fees (1).yml` и т.п.
- **Максимальный размер файла — 1 MB**.
- Файл должен быть корректным YAML (правильные отступы, без табов).

### Пошаговая инструкция загрузки (на примере `fees.yml`)

1. Скачайте текущий файл:
   - `/get_fees`

2. Отредактируйте файл на устройстве.

3. Начните загрузку новой версии:
   - `/set_fees`

4. Бот попросит отправить файл. Прикрепите документом **ровно `fees.yml`**.

5. Дождитесь подтверждения:
   - бот проверит имя, размер, YAML-синтаксис и обязательные поля
   - перед заменой создаст резервную копию вида `fees.yml.backup.YYYYMMDD_HHMMSS`

6. Примените изменения:
   - `/reload_configs`

После `/reload_configs` новые настройки начинают использоваться сразу.

### Команды загрузки для всех файлов

- `/set_fees` — загрузить новый `fees.yml`
- `/set_commissions` — загрузить новый `commissions.yml`
- `/set_rates` — загрузить новый `rates.yml`
- `/set_duties` — загрузить новый `duties.yml`

### Отмена загрузки

Если вы начали загрузку не того файла или передумали:

- `/cancel`

---

## Проверка статуса и синхронизации

### Проверить текущий статус

- `/config_status`

Бот покажет общий «хэш» конфигурации, дату загрузки и список файлов с размерами.

### Проверить, применены ли изменения

- `/config_diff`

Возможные варианты:

- ✅ «Up to date» — всё применено, память и диск синхронизированы
- ⚠️ «Out of sync» — файл обновили, но **ещё не применили**. Нужно:
  - `/reload_configs`

---

## Частые ошибки и быстрые решения

### 1) 🚫 Access Denied

**Причина:** нет прав администратора.

**Что делать:**
1. `/whoami`
2. передайте User ID администратору
3. дождитесь, пока вас добавят в `ADMIN_USER_IDS` (может потребоваться перезапуск бота со стороны команды сопровождения)

### 2) ❌ Неверное имя файла

**Причина:** бот ожидает строгое имя, например `fees.yml`, а пришло `fees (1).yml`.

**Что делать:**
- переименуйте файл в точности как нужно и загрузите снова

### 3) ❌ Ошибка YAML / Validation failed

**Причина:** неправильные отступы/символы в YAML.

**Что делать:**
- проверьте YAML валидатором (например, онлайн)
- убедитесь, что отступы — пробелами (обычно 2 пробела), табы не используйте

### 4) Изменения «не работают»

**Причина:** забыли применить обновлённый конфиг.

**Что делать:**
1. `/config_diff`
2. если «Out of sync» → `/reload_configs`
3. перепроверьте `/config_status`

---

## Рекомендации (чтобы всё прошло гладко)

- Всегда сначала скачивайте текущий файл (`/get_*`), потом редактируйте.
- Делайте небольшие изменения и фиксируйте, что именно поменяли.
- После каждой загрузки обязательно выполняйте `/reload_configs`.
- Не загружайте конфиги в «пиковое время» без необходимости.

---

## Заключение

Через Telegram-бота вы можете безопасно управлять ключевыми настройками расчётов: скачивать, менять и загружать конфиги без доступа к серверу.

Если что-то пошло не так — чаще всего помогает проверка `/config_diff` и команда `/reload_configs`, а при проблемах с доступом — `/whoami` и запрос администратору на добавление вашего User ID.

