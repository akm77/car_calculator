# WebApp Refactoring - Готовые промпты для спринтов

> Быстрый справочник: копируй промпт и давай AI модели для выполнения спринта

---

## 🏃 СПРИНТ 0: Подготовка инфраструктуры

```
Выполни СПРИНТ 0: Подготовка инфраструктуры

**Контекст проекта:**
- Проект: car_calculator (FastAPI + Telegram Bot + WebApp)
- Текущая задача: Рефакторинг webapp/index.html (1548 строк) на модульную структуру
- Методика: RPG (Repository Planning Graph)
- Принцип: Избегаем оверинжиниринга, используем vanilla JS + ES6 модули

**Источники правды (Sources of Truth):**
1. `docs/webapp_refactoring_plan.md` - детальный план (Этап 0)
2. `docs/rpg.yaml` - граф проекта (раздел app_webapp)
3. `app/webapp/index.html` - текущее монолитное состояние

**Роль:**
Ты — инфраструктурный инженер, создающий базовую структуру для модульного фронтенд приложения на vanilla JavaScript с ES6 модулями.

**Задачи на спринт:**
1. Создать структуру папок:
   - app/webapp/js/config/
   - app/webapp/js/utils/
   - app/webapp/js/modules/
   - app/webapp/css/

2. Создать файл бэкапа:
   cp app/webapp/index.html app/webapp/index.html.backup

3. Создать app/webapp/js/README.md с описанием структуры модулей

4. Обновить app/main.py для раздачи новых статических файлов (/static/css/, /static/js/)

**Критерии достижения цели:**
- ✅ Все папки созданы
- ✅ Бэкап index.html существует
- ✅ FastAPI раздаёт статику из новых папок
- ✅ Нет ошибок в терминале при запуске

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить информацию о новой структуре
- CHANGELOG_georgia.md - новая запись о начале рефакторинга
```

---

## 🎨 СПРИНТ 1: Извлечение CSS

```
Выполни СПРИНТ 1: Извлечение CSS

**Контекст проекта:**
- Проект: car_calculator (FastAPI + Telegram Bot + WebApp)
- Текущая задача: Вынос CSS из монолитного HTML в отдельные модули
- Методика: RPG (Repository Planning Graph)

**Источники правды:**
1. `app/webapp/index.html` (строки 1-300) - секция <style> с CSS
2. `docs/webapp_refactoring_plan.md` - Этап 1
3. Telegram Design Guidelines для стилей

**Роль:**
Ты — CSS-архитектор, разделяющий монолитные стили на логические модули с использованием CSS переменных и БЭМ-подобной методологии.

**Задачи на спринт:**
1. Создать app/webapp/css/variables.css:
   - CSS-переменные для цветов (Telegram theme colors)
   - Переменные для размеров (шрифты, отступы, радиусы)

2. Создать app/webapp/css/base.css:
   - CSS reset/normalize
   - Базовые стили body, html
   - Стили контейнера .container
   - Типографика (h1, h2, p)

3. Создать app/webapp/css/components.css:
   - Кнопки (.btn, .btn-primary, .btn-telegram)
   - Карточки (.card, .result-card)
   - Формы (.form-group, .input, .select)
   - Breakdown items (.breakdown-item)

4. Создать app/webapp/css/telegram.css:
   - Telegram WebApp специфичные стили
   - Адаптация под тёмную тему

5. Обновить app/webapp/index.html:
   - Удалить <style>...</style>
   - Добавить <link> на все 4 CSS файла

**Критерии достижения цели:**
- ✅ В index.html нет тега <style>
- ✅ Все 4 CSS файла подключены
- ✅ WebApp открывается без визуальных изменений
- ✅ Нет ошибок в консоли браузера
- ✅ Работает переключение тёмной темы

**После завершения:**
Обнови:
- docs/webapp_refactoring_checklist.md - отметить Этап 1
- CHANGELOG_georgia.md - запись о выносе CSS
```

---

## 🛠️ СПРИНТ 2: Утилиты форматирования и DOM

```
Выполни СПРИНТ 2: Утилиты форматирования и DOM

**Контекст проекта:**
- Проект: car_calculator
- Задача: Создание библиотеки утилит (formatters, DOM helpers)
- Методика: RPG - чистые функции (pure functions)

**Источники правды:**
1. `app/webapp/index.html` (строки 300-800) - функции formatNumber(), show(), hide()
2. `docs/webapp_refactoring_plan.md` - Этап 2
3. `docs/rpg.yaml` - модуль app_calculation/models.py

**Роль:**
Ты — разработчик библиотеки утилит, создающий чистые функции для форматирования данных и работы с DOM без внешних зависимостей.

**Задачи на спринт:**
1. Создать app/webapp/js/utils/formatters.js:
   - formatNumber(num) → строка с разделителями
   - formatCurrency(amount, currency) → форматированная валюта
   - getAgeCategory(category) → человекочитаемая метка
   - formatEngineVolume(cc) → "1500 см³"

2. Создать app/webapp/js/utils/dom.js:
   - show(element) → добавить класс .show
   - hide(element) → убрать класс .show
   - setContent(elementId, html) → innerHTML
   - setText(elementId, text) → textContent
   - debounce(fn, delay) → дебаунс функция

3. Обновить app/webapp/index.html:
   - Добавить <script type="module"> импорты
   - Заменить прямые вызовы на импортированные функции

4. Создать тест tests/manual/test_formatters.html

**Критерии достижения цели:**
- ✅ Файлы formatters.js и dom.js содержат только чистые функции
- ✅ В index.html нет дублирования функций
- ✅ WebApp работает без изменений
- ✅ test_formatters.html показывает ✅ для всех тестов
- ✅ Нет ошибок импорта модулей

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить компоненты formatters.js и dom.js
- docs/webapp_refactoring_checklist.md - Этап 2
- CHANGELOG_georgia.md
```

---

## ⚙️ СПРИНТ 3: Константы и конфигурация

```
Выполни СПРИНТ 3: Константы и конфигурация

**Контекст проекта:**
- Проект: car_calculator
- Задача: Выделение всех магических чисел и строк в единые источники правды
- Методика: RPG - Single Source of Truth

**Источники правды:**
1. `app/webapp/index.html` (строки 800-1200) - хардкод строк и чисел
2. `app/calculation/models.py` - константы валидации
3. `docs/webapp_refactoring_plan.md` - Этап 3
4. `app/core/messages.py` - серверные сообщения

**Роль:**
Ты — конфигурационный менеджер, выделяющий все магические числа и строки в единые источники правды для упрощения поддержки.

**Задачи на спринт:**
1. Создать app/webapp/js/config/messages.js:
   - errors: тексты ошибок валидации
   - buttons: тексты кнопок
   - labels: метки полей
   - info: информационные сообщения
   - warnings: предупреждения

2. Создать app/webapp/js/config/constants.js:
   - Constraints: лимиты валидации (YEAR_MIN, YEAR_MAX, ENGINE_CC_MIN)
   - API_ENDPOINTS: пути к API
   - API_CONFIG: retry, timeout, delay
   - DEFAULT_VALUES: дефолты для формы
   - COUNTRY_EMOJI: эмодзи стран

3. Синхронизировать с бэкендом:
   - Убедиться что MIN_YEAR, MAX_CC совпадают с models.py

4. Обновить app/webapp/index.html:
   - Импортировать Messages и Constants
   - Заменить хардкод на импорты

**Критерии достижения цели:**
- ✅ Нет строковых литералов в index.html
- ✅ Нет магических чисел
- ✅ Константы совпадают с app/calculation/models.py
- ✅ WebApp показывает те же тексты
- ✅ Легко поменять текст (1 файл, 1 строка)

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить узлы messages.js и constants.js
- docs/webapp_refactoring_checklist.md - Этап 3
- CHANGELOG_georgia.md
```

---

## ✅ СПРИНТ 4: Модуль валидации форм

```
Выполни СПРИНТ 4: Модуль валидации форм

**Контекст проекта:**
- Проект: car_calculator
- Задача: Единый модуль валидации с поддержкой real-time проверки
- Методика: RPG - единый источник правил валидации

**Источники правды:**
1. `app/webapp/index.html` (строки 900-1100) - функция validateForm()
2. `app/calculation/models.py` - класс CalculationRequest с Pydantic
3. `docs/webapp_refactoring_plan.md` - Этап 4
4. `app/webapp/js/config/constants.js` - константы лимитов

**Роль:**
Ты — инженер по валидации данных, создающий единый модуль валидации с поддержкой полной валидации и валидации отдельных полей.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/validator.js:
   - Класс FormValidator
   - validate(formData) → {isValid, errors[]}
   - validateField(name, value) → error | null
   - getFieldConstraints(name) → {min, max, pattern}

2. Реализовать правила:
   - year: YEAR_MIN <= year <= YEAR_MAX()
   - engine_cc: ENGINE_CC_MIN <= cc <= ENGINE_CC_MAX
   - purchase_price: price > 0
   - country: не пустое

3. Добавить кастомные валидаторы:
   - addCustomValidator(fieldName, fn)

4. Обновить index.html:
   - Заменить validateForm() на validator.validate()
   - Добавить real-time валидацию на input события

5. Создать test_validator.html

**Критерии достижения цели:**
- ✅ FormValidator работает изолированно
- ✅ Валидация использует FormValidator
- ✅ Real-time валидация работает
- ✅ Все тесты проходят
- ✅ Нет дублирования правил

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить компонент FormValidator
- docs/webapp_refactoring_checklist.md - Этап 4
- CHANGELOG_georgia.md
```

---

## 🌐 СПРИНТ 5: Улучшенный API клиент

```
Выполни СПРИНТ 5: Улучшенный API клиент

**Контекст проекта:**
- Проект: car_calculator
- Задача: HTTP клиент с retry, timeout и улучшенной обработкой ошибок
- Методика: RPG - надёжная работа с сетью

**Источники правды:**
1. `app/webapp/index.html` (строки 600-800) - текущий APIClient
2. `app/services/cbr.py` - пример retry логики с @retry
3. `docs/webapp_refactoring_plan.md` - Этап 5
4. `app/webapp/js/config/constants.js` - API_CONFIG

**Роль:**
Ты — инженер надёжности сети, создающий HTTP клиент с retry, таймаутами и улучшенной обработкой ошибок для нестабильных соединений.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/api.js:
   - Класс APIError (кастомная ошибка)
   - Класс APIClient
   - get(path), post(path, data)
   - fetchWithTimeout(url, options, timeout)
   - fetchWithRetry(url, options, maxRetries)
   - Специфичные: calculate(), getMeta(), getRates(), refreshRates()

2. Реализовать retry:
   - Только на сетевых ошибках (не 4xx/5xx)
   - Экспоненциальная задержка
   - Максимум из API_CONFIG.RETRY_COUNT

3. Реализовать таймауты:
   - AbortController
   - Таймаут из API_CONFIG.TIMEOUT
   - APIError с кодом 408

4. Улучшить обработку ошибок:
   - Парсить FastAPI {"detail": "..."}
   - Типы: NetworkError, TimeoutError, ValidationError
   - Логирование с timestamp

5. Обновить index.html:
   - Заменить старый APIClient
   - Обработать APIError в UI

**Критерии достижения цели:**
- ✅ APIClient работает изолированно
- ✅ Retry срабатывает (проверить через DevTools throttling)
- ✅ Timeout срабатывает
- ✅ Ошибки 4xx понятны пользователю
- ✅ WebApp обрабатывает все ошибки

**После завершения:**
Обнови:
- docs/rpg.yaml - обновить APIClient
- docs/webapp_refactoring_checklist.md - Этап 5
- CHANGELOG_georgia.md
```

---

## 🎨 СПРИНТ 6: Модуль управления UI

```
Выполни СПРИНТ 6: Модуль управления UI

**Контекст проекта:**
- Проект: car_calculator
- Задача: Централизованное управление состояниями UI (loading, error, success)
- Методика: RPG - абстракция над DOM

**Источники правды:**
1. `app/webapp/index.html` (строки 1000-1300) - showError(), showLoading(), showResult()
2. `docs/webapp_refactoring_plan.md` - Этап 6
3. `app/webapp/js/utils/dom.js` - базовые DOM утилиты

**Роль:**
Ты — UI инженер, создающий абстракцию над DOM для централизованного управления состояниями интерфейса.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/ui.js:
   - Класс UI
   - showError(message), hideError()
   - showLoading(text), hideLoading()
   - showResult(), hideResult()
   - showShareButton(), hideShareButton()
   - scrollToResult()
   - disableForm(), enableForm()

2. Добавить state management:
   - Внутреннее состояние _state (idle, loading, error, success)
   - getState() для отладки
   - State transitions

3. Добавить анимации:
   - Плавное появление/скрытие (CSS transitions)
   - Scroll с анимацией
   - Telegram Haptic Feedback

4. Добавить accessibility:
   - ARIA атрибуты
   - Focus management

5. Обновить index.html:
   - Заменить все showError() на ui.showError()
   - Убрать дублирование

**Критерии достижения цели:**
- ✅ UI управляет всеми видимыми состояниями
- ✅ Нет прямых вызовов show()/hide()
- ✅ Анимации работают плавно
- ✅ Haptic feedback в Telegram
- ✅ Accessibility проверен

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить компонент UI
- docs/webapp_refactoring_checklist.md - Этап 6
- CHANGELOG_georgia.md
```

---

## 📊 СПРИНТ 7: Рендеринг результатов

```
Выполни СПРИНТ 7: Рендеринг результатов расчёта

**Контекст проекта:**
- Проект: car_calculator
- Задача: Модуль для отображения результатов с breakdown, meta, warnings
- Методика: RPG - структурированный рендеринг

**Источники правды:**
1. `app/webapp/index.html` (строки 1200-1500) - displayResult(result)
2. `app/calculation/models.py` - CalculationResult, CostBreakdown, CalculationMeta
3. `docs/webapp_refactoring_plan.md` - Этап 7
4. `app/api/routes.py` - структура /api/meta countries

**Роль:**
Ты — специалист по рендерингу данных, создающий модуль для отображения результатов расчёта.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/results.js:
   - Класс ResultsRenderer
   - render(result)
   - renderTotal(breakdown)
   - renderBreakdown(breakdown)
   - renderMeta(meta, request)
   - createBreakdownItem(label, amount, isTotal)
   - getCountryLabel(code) → emoji + label
   - addDutyInfo(parts, meta)
   - addWarnings(parts, meta)

2. Реализовать рендеринг breakdown:
   - Показывать только ненулевые статьи
   - Форматировать через formatNumber()
   - Подсветка total

3. Реализовать рендеринг meta:
   - Страна с эмодзи
   - Возраст и категория
   - Объём или volume_band
   - Детали пошлины

4. Реализовать warnings:
   - Иконки (⚠️, ℹ️)
   - Цветовое кодирование
   - Warnings из meta.warnings[]

5. Обновить index.html:
   - Заменить displayResult() на renderer.render()

**Критерии достижения цели:**
- ✅ Результаты в том же формате
- ✅ Все breakdown статьи корректны
- ✅ Meta-информация полная
- ✅ Warnings с правильными цветами
- ✅ Код изолирован

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить ResultsRenderer
- docs/webapp_refactoring_checklist.md - Этап 7
- CHANGELOG_georgia.md
```

---

## 📱 СПРИНТ 8: Telegram WebApp хелпер

```
Выполни СПРИНТ 8: Telegram WebApp хелпер

**Контекст проекта:**
- Проект: car_calculator
- Задача: Обёртка над Telegram WebApp API
- Методика: RPG - упрощение интеграции

**Источники правды:**
1. `app/webapp/index.html` (строки 200-400) - window.Telegram.WebApp
2. Telegram WebApp Docs: https://core.telegram.org/bots/webapps
3. `docs/webapp_refactoring_plan.md` - упоминания Telegram
4. `app/bot/keyboards.py` - WebApp button

**Роль:**
Ты — интеграционный специалист Telegram, создающий обёртку над WebApp API для упрощения работы с кнопками, темами и haptic feedback.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/telegram.js:
   - Класс TelegramWebAppHelper
   - isInTelegram(), ready(), expand(), close()
   - Main Button: show, hide, setLoading, onClick
   - Back Button: show, hide, onClick
   - Theme: getThemeParams(), isDarkTheme(), onThemeChanged()
   - Feedback: hapticFeedback(type)
   - Data: sendData(data)

2. Реализовать fallback для браузера:
   - Если не в Telegram, методы не падают (no-op)
   - isInTelegram() возвращает false

3. Добавить обработку ошибок:
   - Проверка window.Telegram.WebApp
   - Graceful degradation

4. Обновить index.html:
   - Заменить window.Telegram.WebApp на хелпер
   - Добавить haptic feedback на действия

**Критерии достижения цели:**
- ✅ WebApp работает в Telegram
- ✅ Main Button показывается
- ✅ Back Button работает
- ✅ Haptic feedback срабатывает
- ✅ WebApp работает в браузере без ошибок

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить TelegramWebAppHelper
- docs/webapp_refactoring_checklist.md - Telegram интеграция
- CHANGELOG_georgia.md
```

---

## 🎯 СПРИНТ 9: Главный контроллер калькулятора

```
Выполни СПРИНТ 9: Главный контроллер калькулятора

**Контекст проекта:**
- Проект: car_calculator
- Задача: Оркестрация всех модулей в единый workflow
- Методика: RPG - dependency injection и orchestration

**Источники правды:**
1. `app/webapp/index.html` (строки 1400-1548) - calculateCost()
2. `docs/webapp_refactoring_plan.md` - Этап 8
3. Все модули из спринтов 4-8
4. `docs/webapp_dependency_graph.md`

**Роль:**
Ты — архитектор приложения, создающий главный контроллер который оркестрирует API, Validator, UI, Results, Telegram в единый workflow.

**Задачи на спринт:**
1. Создать app/webapp/js/modules/calculator.js:
   - Класс CalculatorController
   - constructor(telegram, metaData)
   - setCountry(country)
   - setFreightType(freightType)
   - async handleSubmit(form)
   - buildRequest(formData)
   - updateTelegramButton()
   - reset()

2. Реализовать dependency injection:
   - this.api = new APIClient()
   - this.validator = new FormValidator()
   - this.ui = new UI()
   - this.resultsRenderer = new ResultsRenderer(metaData)
   - this.telegram = telegram

3. Реализовать workflow:
   handleSubmit():
     1. Проверить страну
     2. Валидировать форму
     3. Показать loading
     4. Отправить запрос
     5. Отобразить результат
     6. Обновить Telegram кнопки
     7. Обработать ошибки

4. Добавить state management:
   - selectedCountry
   - selectedFreightType
   - lastResult

5. Обновить index.html:
   - Создать CalculatorController в init()
   - Подключить события к calculator.handleSubmit()
   - Убрать бизнес-логику

**Критерии достижения цели:**
- ✅ Flow через CalculatorController
- ✅ В index.html нет бизнес-логики
- ✅ Расчёт работает (все страны)
- ✅ Обработка ошибок корректна
- ✅ Telegram интеграция работает

**После завершения:**
Обнови:
- docs/rpg.yaml - добавить CalculatorController
- docs/webapp_refactoring_checklist.md - Этап 8
- docs/webapp_dependency_graph.md - граф с контроллером
- CHANGELOG_georgia.md
```

---

## 🏁 СПРИНТ 10: Финализация - минимизация index.html

```
Выполни СПРИНТ 10: Финализация - минимизация index.html

**Контекст проекта:**
- Проект: car_calculator
- Задача: Завершение миграции, минимизация HTML до структуры
- Методика: RPG - clean HTML с модульным JS

**Источники правды:**
1. `app/webapp/index.html` - текущее (после спринтов 1-9)
2. `docs/webapp_refactoring_plan.md` - Этап 9
3. `app/webapp/index.html.backup` - оригинал для сравнения

**Роль:**
Ты — рефакторинг-специалист, завершающий миграцию путём минимизации index.html до чистого HTML/CSS.

**Задачи на спринт:**
1. Очистить index.html:
   - Убрать все <script> блоки кроме bootstrap
   - Оставить только структурный HTML
   - <head> с мета-тегами и CSS
   - <body> с формой и результатами

2. Создать bootstrap скрипт:
   <script type="module">
     import { CalculatorController } from './js/modules/calculator.js';
     // ...init логика
   </script>

3. Создать app/webapp/js/modules/init.js:
   - setupEventListeners()
   - populateCountries()

4. Финальная проверка:
   - Удалить закомментированный код
   - Проверить нет дублирования
   - Все импорты корректны

5. Сравнить с бэкапом:
   - Функциональность та же
   - Строк: было 1548 → стало ~200 + модули
   - Все тесты проходят

**Критерии достижения цели:**
- ✅ index.html ~150-200 строк
- ✅ Весь JS в модулях
- ✅ Весь CSS в файлах
- ✅ WebApp работает идентично
- ✅ Все страны работают
- ✅ Telegram интеграция работает
- ✅ tests/functional/test_api.py проходят

**После завершения:**
Обнови:
- docs/webapp_refactoring_summary.md - отметить завершение
- docs/webapp_refactoring_checklist.md - все этапы выполнены
- docs/rpg.yaml - обновить app_webapp
- CHANGELOG_georgia.md - итоговая запись
- Создать docs/webapp_architecture.md
```

---

## 🧪 СПРИНТ 11: Базовые unit-тесты (ОПЦИОНАЛЬНО)

```
Выполни СПРИНТ 11: Базовые unit-тесты (опционально)

**Контекст проекта:**
- Проект: car_calculator
- Задача: Простые unit-тесты без фреймворков (нативный JS в браузере)
- Методика: RPG - тестирование утилит и модулей

**Источники правды:**
1. `docs/webapp_refactoring_plan.md` - Этап 10
2. Все модули из спринтов 2-9
3. `tests/functional/test_api.py` - примеры тестов

**Роль:**
Ты — QA инженер, создающий простые unit-тесты для утилит и модулей без сложных фреймворков.

**Задачи на спринт:**
1. Создать tests/manual/test_utils.html:
   - Тесты для formatters.js
   - Тесты для dom.js (debounce, throttle)

2. Создать tests/manual/test_validator.html:
   - Тесты для FormValidator
   - Валидные и невалидные данные

3. Создать tests/manual/test_api.html:
   - Mock API responses
   - Тесты retry логики
   - Тесты timeout

4. Создать tests/manual/test_renderer.html:
   - Тесты рендеринга breakdown
   - Тесты рендеринга meta
   - Тесты warnings

5. Создать tests/manual/README.md:
   - Инструкция запуска
   - Список всех тестов
   - Ожидаемые результаты

**Критерии достижения цели:**
- ✅ Все тесты проходят в браузере
- ✅ Тесты покрывают основные утилиты
- ✅ Легко запустить (открыть HTML)
- ✅ Нет внешних зависимостей

**После завершения:**
Обнови:
- docs/rpg.yaml - обновить test coverage для webapp
- docs/webapp_refactoring_checklist.md - тестирование
- CHANGELOG_georgia.md
```

---

## 📋 Порядок выполнения

**Последовательные спринты** (обязательный порядок):
1. Спринт 0 → Спринт 1 → Спринт 2 → ... → Спринт 10

**Параллельные возможности** (если работает команда):
- Спринты 2 и 3 можно делать параллельно
- Спринты 5 и 6 можно делать параллельно
- Спринты 7 и 8 можно делать параллельно

**Критический путь** (минимум):
S0 → S1 → S2 → S4 → S9 → S10 = 13-19 часов

**Общее время**: 22-35 часов (3-5 рабочих дней)

---

## 💡 Советы по работе со спринтами

1. **Копируй промпт полностью** - всё необходимое уже включено
2. **Проверяй критерии** после каждого спринта
3. **Коммить после каждого спринта**: `git commit -m "refactor(webapp): sprint N - description"`
4. **Тестируй в браузере И в Telegram** после каждого спринта
5. **Обновляй документацию** - это важно для следующих спринтов
6. **Если спринт не укладывается в контекст** - разбей его на 2 части
7. **Используй бэкап** если что-то пошло не так: `cp app/webapp/index.html.backup app/webapp/index.html`

---

## 🎯 Метрики успеха (после всех спринтов)

- ✅ index.html: с 1548 до ~200 строк
- ✅ Время добавления страны: с 4ч до 30 мин
- ✅ Количество модулей: ~10 файлов
- ✅ Тестовое покрытие: >80% для утилит
- ✅ Дублирование кода: 0%
- ✅ Lighthouse Score: ≥90

