# WebApp Refactoring Progress Tracker

**Дата начала**: _____________  
**Дата завершения**: _____________  
**Исполнитель**: _____________  

---

## 📋 Этап 0: Подготовка (1-2 часа)

- [ ] Прочитана документация:
  - [ ] `docs/webapp_refactoring_summary.md`
  - [ ] `docs/webapp_refactoring_plan.md`
  - [ ] `docs/webapp_dependency_graph.md`

- [ ] Запущен скрипт подготовки:
  ```bash
  ./scripts/webapp_refactoring_init.sh
  ```

- [ ] Создана структура папок:
  - [ ] `app/webapp/css/`
  - [ ] `app/webapp/js/config/`
  - [ ] `app/webapp/js/utils/`
  - [ ] `app/webapp/js/modules/`

- [ ] Создан бэкап:
  - [ ] `app/webapp/index.html.backup`

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 0 - подготовка структуры"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 1: Вынос CSS (2-3 часа)

- [x] Создан `app/webapp/css/variables.css`:
  - [x] CSS переменные (--bg-color, --text-color, etc.)
  
- [x] Создан `app/webapp/css/base.css`:
  - [x] Reset стили (* { margin: 0; })
  - [x] body, container, header стили

- [x] Создан `app/webapp/css/components.css`:
  - [x] form-card, form-group
  - [x] buttons (.calculate-btn, .back-btn)
  - [x] input, select стили
  - [x] result-card стили

- [x] Создан `app/webapp/css/telegram.css`:
  - [x] Telegram-специфичные стили
  - [x] Tab navigation

- [x] Обновлен `app/webapp/index.html`:
  - [x] Удален `<style>...</style>` блок
  - [x] Добавлены `<link>` теги на CSS файлы

- [x] Проверка:
  - [x] Открыт в браузере - стили работают
  - [x] Telegram WebApp - стили применяются
  - [x] Нет ошибок в консоли

- [x] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 1 - вынос CSS в отдельные файлы"
  ```

**Статус**: ✅ Завершено  
**Время фактическое**: 2 часа  
**Дата завершения**: December 5, 2025

---

## 📋 Этап 2: Утилиты (3-4 часа)

- [ ] Создан `app/webapp/js/utils/formatters.js`:
  - [ ] `formatNumber(num)`
  - [ ] `formatCurrency(amount, currency)`
  - [ ] `getAgeCategory(category)`
  - [ ] export функций

- [ ] Создан `app/webapp/js/utils/dom.js`:
  - [ ] `show(element)`
  - [ ] `hide(element)`
  - [ ] `setText(elementId, text)`
  - [ ] `setContent(elementId, html)`
  - [ ] `debounce(fn, delay)`
  - [ ] export функций

- [ ] Обновлен `index.html`:
  - [ ] Импортированы утилиты
  - [ ] Заменены вызовы на импортированные функции

- [ ] Проверка:
  - [ ] formatNumber работает (числа форматируются)
  - [ ] show/hide работают (элементы показываются/скрываются)
  - [ ] debounce работает (задержка срабатывает)

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 2 - вынос утилит"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 3: Конфигурация (1-2 часа)

- [ ] Создан `app/webapp/js/config/messages.js`:
  - [ ] `Messages.errors` (все сообщения об ошибках)
  - [ ] `Messages.buttons` (тексты кнопок)
  - [ ] `Messages.labels` (метки полей)
  - [ ] export Messages

- [ ] Создан `app/webapp/js/config/constants.js`:
  - [ ] `Constraints` (YEAR_MIN, ENGINE_CC_MIN, etc.)
  - [ ] `API_ENDPOINTS` (/api/calculate, etc.)
  - [ ] `API_CONFIG` (RETRY_COUNT, TIMEOUT)
  - [ ] `DEFAULT_VALUES` (FREIGHT_TYPE, etc.)
  - [ ] export всех констант

- [ ] Обновлен код:
  - [ ] Заменены хардкод строки на Messages
  - [ ] Заменены магические числа на Constraints
  - [ ] Заменены URL на API_ENDPOINTS

- [ ] Проверка:
  - [ ] Сообщения об ошибках отображаются
  - [ ] Валидация использует константы
  - [ ] API запросы идут на правильные эндпоинты

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 3 - вынос конфигурации"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 4: Валидация (2-3 часа)

- [ ] Создан `app/webapp/js/modules/validator.js`:
  - [ ] Класс `FormValidator`
  - [ ] Метод `validate(formData)` → ValidationResult
  - [ ] Метод `validateField(name, value)` → string | null
  - [ ] Валидация года (min, max, future)
  - [ ] Валидация объёма двигателя (500-10000)
  - [ ] Валидация цены (> 0)
  - [ ] export FormValidator

- [ ] Обновлен код:
  - [ ] Создан экземпляр `validator = new FormValidator()`
  - [ ] Заменена функция `validateForm()` на `validator.validate()`
  - [ ] Обработка `ValidationResult.errors`

- [ ] Проверка:
  - [ ] Валидация года работает (2030 → ошибка)
  - [ ] Валидация объёма работает (100 → ошибка)
  - [ ] Валидация цены работает (0 → ошибка)
  - [ ] Множественные ошибки показываются

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 4 - модуль валидации"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 5: API клиент (2-3 часа)

- [ ] Создан `app/webapp/js/modules/api.js`:
  - [ ] Класс `APIError extends Error`
  - [ ] Класс `APIClient`
  - [ ] Метод `fetchWithTimeout()` (AbortController)
  - [ ] Метод `fetchWithRetry()` (retry логика)
  - [ ] Метод `calculate(request)` → Promise<CalculationResult>
  - [ ] Метод `getMeta()` → Promise<MetaData>
  - [ ] Метод `getRates()` → Promise<RatesData>
  - [ ] export APIClient, APIError

- [ ] Обновлен код:
  - [ ] Создан `api = new APIClient()`
  - [ ] Заменены вызовы `api.post('/api/calculate')` на `api.calculate()`
  - [ ] Обработка `APIError` (status, message)

- [ ] Проверка:
  - [ ] Успешный запрос работает
  - [ ] Timeout срабатывает (имитация медленного API)
  - [ ] Retry работает (имитация сбоя сети)
  - [ ] 4xx/5xx ошибки обрабатываются

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 5 - улучшенный API клиент"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 6: UI модуль (3-4 часа)

- [ ] Создан `app/webapp/js/modules/ui.js`:
  - [ ] Класс `UI`
  - [ ] Метод `showError(message)`
  - [ ] Метод `hideError()`
  - [ ] Метод `showLoading(text?)`
  - [ ] Метод `hideLoading()`
  - [ ] Метод `showResult()`
  - [ ] Метод `hideResult()`
  - [ ] Метод `showShareButton()` / `hideShareButton()`
  - [ ] export UI

- [ ] Обновлен код:
  - [ ] Создан `ui = new UI()`
  - [ ] Заменены функции `showError()` на `ui.showError()`
  - [ ] Заменены функции `showLoading()` на `ui.showLoading()`
  - [ ] Заменены функции `hideResult()` на `ui.hideResult()`

- [ ] Проверка:
  - [ ] Ошибки показываются/скрываются
  - [ ] Спиннер загрузки работает
  - [ ] Карточка результата показывается
  - [ ] Кнопка "Поделиться" показывается

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 6 - UI модуль"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 7: Results renderer (3-4 часа)

- [ ] Создан `app/webapp/js/modules/results.js`:
  - [ ] Класс `ResultsRenderer`
  - [ ] Метод `render(result)`
  - [ ] Метод `renderTotal(breakdown)`
  - [ ] Метод `renderBreakdown(breakdown)`
  - [ ] Метод `renderMeta(meta, request)`
  - [ ] Метод `createBreakdownItem(label, amount, isTotal?)`
  - [ ] Метод `getEngineDisplay(meta, request)`
  - [ ] Метод `addDutyInfo(parts, meta)`
  - [ ] Метод `addWarnings(parts, meta)`
  - [ ] Метод `getCountryLabel(code)`
  - [ ] Метод `getAgeCategory(category)`
  - [ ] export ResultsRenderer

- [ ] Обновлен код:
  - [ ] Создан `resultsRenderer = new ResultsRenderer(metaData)`
  - [ ] Заменена функция `displayResult()` на `resultsRenderer.render()`

- [ ] Проверка:
  - [ ] Итоговая сумма отображается
  - [ ] Breakdown с компонентами отображается
  - [ ] Метаданные (возраст, объём, пошлина) отображаются
  - [ ] Warnings отображаются

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 7 - results renderer"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 8: Calculator controller (3-4 часа)

- [ ] Создан `app/webapp/js/modules/calculator.js`:
  - [ ] Класс `CalculatorController`
  - [ ] Constructor (api, validator, ui, resultsRenderer, telegram)
  - [ ] Свойства: selectedCountry, selectedFreightType
  - [ ] Метод `setCountry(country)`
  - [ ] Метод `setFreightType(freightType)`
  - [ ] Метод `handleSubmit(form)` (главная логика)
  - [ ] Метод `buildRequest(formData)`
  - [ ] Метод `updateTelegramButton()`
  - [ ] export CalculatorController

- [ ] Обновлен код:
  - [ ] Создан `calculator = new CalculatorController(telegram, metaData)`
  - [ ] Заменена функция `calculateCost()` на `calculator.handleSubmit(form)`
  - [ ] Заменена `selectCountry()` на `calculator.setCountry()`
  - [ ] Заменена `updateMainButtonState()` на `calculator.updateTelegramButton()`

- [ ] Проверка:
  - [ ] Форма отправляется и результат показывается
  - [ ] Валидация работает перед отправкой
  - [ ] Ошибки API обрабатываются
  - [ ] Telegram MainButton обновляется

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 8 - calculator controller"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 9: Обновить HTML (2-3 часа)

- [ ] Обновлен `app/webapp/index.html`:
  - [ ] Минимизирован `<script>` блок
  - [ ] Добавлен `type="module"` к script
  - [ ] Импортированы все модули
  - [ ] Создана функция `init()`
  - [ ] Создана функция `setupEventListeners()`
  - [ ] Вызов `init()` при загрузке
  - [ ] Удалены старые глобальные функции

- [ ] Проверка структуры:
  - [ ] HTML < 200 строк
  - [ ] JS логика в модулях
  - [ ] Нет дублирования кода

- [ ] Полное тестирование:
  - [ ] Выбор страны работает
  - [ ] Заполнение формы работает
  - [ ] Валидация срабатывает
  - [ ] Расчёт выполняется
  - [ ] Результат отображается
  - [ ] Telegram кнопки работают
  - [ ] Поделиться работает

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 9 - минимизация HTML"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 10: Базовые тесты (2-3 часа, опционально)

- [ ] Создан `tests/unit/test_formatters.html`:
  - [ ] Тест `formatNumber` с числами
  - [ ] Тест `formatNumber` с null/undefined
  - [ ] Тест `getAgeCategory` с категориями

- [ ] Создан `tests/unit/test_validator.html`:
  - [ ] Тест валидации года (будущий год)
  - [ ] Тест валидации года (< 1990)
  - [ ] Тест валидации объёма (< 500)
  - [ ] Тест валидации объёма (> 10000)
  - [ ] Тест валидации цены (<= 0)
  - [ ] Тест успешной валидации

- [ ] Запуск тестов:
  - [ ] Открыт test_formatters.html - все тесты ✅
  - [ ] Открыт test_validator.html - все тесты ✅

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 10 - базовые unit-тесты"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📊 Финальная проверка

### Код Quality
- [ ] Каждый файл < 300 строк
- [ ] Нет глобальных переменных (кроме telegram, calculator в init)
- [ ] Нет дублирования кода
- [ ] Нет магических чисел/строк
- [ ] Все функции имеют понятные имена
- [ ] Комментарии только где необходимо

### Функциональность
- [ ] Webapp открывается в браузере
- [ ] Webapp работает в Telegram
- [ ] Все страны доступны
- [ ] Все типы фрахта работают
- [ ] Расчёт возвращает правильный результат
- [ ] Результат форматируется корректно
- [ ] Warnings отображаются
- [ ] Поделиться работает

### Performance
- [ ] CSS файлы кэшируются
- [ ] JS модули кэшируются
- [ ] Debounce на input работает
- [ ] Нет зависаний UI

### Документация
- [ ] Обновлён `docs/rpg.yaml` (new components)
- [ ] Создан CHANGELOG.md с изменениями
- [ ] README обновлён (если нужно)

---

## 🎯 Метрики успеха

**Проверь что цели достигнуты**:

| Метрика | Цель | Фактическое | ✅/❌ |
|---------|------|-------------|------|
| Строк в самом большом файле | < 300 | _____ | |
| Время добавления новой страны | < 30 мин | _____ | |
| Количество мест с валидацией | 1 | _____ | |
| Количество мест с Messages | 1 | _____ | |
| Браузерное кэширование CSS/JS | Да | _____ | |
| Время понимания кода новичком | < 30 мин | _____ | |

---

## 📝 Заметки и проблемы

**Проблемы, с которыми столкнулись**:
```
(Записывайте здесь проблемы и их решения)

1. 
2. 
3. 
```

**Улучшения, которые можно сделать в будущем**:
```
(Идеи для следующих итераций)

1. 
2. 
3. 
```

---

## 🎉 Завершение

- [ ] Все этапы завершены
- [ ] Все тесты пройдены
- [ ] Документация обновлена
- [ ] Финальный коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): завершён рефакторинг - модульная структура"
  git push
  ```

**Дата завершения**: _____________  
**Общее время**: _____ часов  
**Статус**: 🎉 ЗАВЕРШЕНО

---

**Следующие шаги**:
1. Мониторинг production в течение недели
2. Сбор обратной связи от команды
3. Планирование следующих улучшений

