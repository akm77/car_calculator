# WebApp Refactoring Progress Tracker

**Дата начала**: _____________  
**Дата завершения**: _____________  
**Исполнитель**: _____________  

---

## 📋 Этап 0: Подготовка (1-2 часа)

- [ ] Прочитана документация:
  - [ ] `docs/webapp_refactoring_summary.md`
  - [ ] `docs/webapp_refactoring_plan.md`
  - [ ] `docs/webapp_dependency_graph.md`

- [ ] Запущен скрипт подготовки:
  ```bash
  ./scripts/webapp_refactoring_init.sh
  ```

- [ ] Создана структура папок:
  - [ ] `app/webapp/css/`
  - [ ] `app/webapp/js/config/`
  - [ ] `app/webapp/js/utils/`
  - [ ] `app/webapp/js/modules/`

- [ ] Создан бэкап:
  - [ ] `app/webapp/index.html.backup`

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 0 - подготовка структуры"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 1: Вынос CSS (2-3 часа)

- [x] Создан `app/webapp/css/variables.css`:
  - [x] CSS переменные (--bg-color, --text-color, etc.)
  
- [x] Создан `app/webapp/css/base.css`:
  - [x] Reset стили (* { margin: 0; })
  - [x] body, container, header стили

- [x] Создан `app/webapp/css/components.css`:
  - [x] form-card, form-group
  - [x] buttons (.calculate-btn, .back-btn)
  - [x] input, select стили
  - [x] result-card стили

- [x] Создан `app/webapp/css/telegram.css`:
  - [x] Telegram-специфичные стили
  - [x] Tab navigation

- [x] Обновлен `app/webapp/index.html`:
  - [x] Удален `<style>...</style>` блок
  - [x] Добавлены `<link>` теги на CSS файлы

- [x] Проверка:
  - [x] Открыт в браузере - стили работают
  - [x] Telegram WebApp - стили применяются
  - [x] Нет ошибок в консоли

- [x] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 1 - вынос CSS в отдельные файлы"
  ```

**Статус**: ✅ Завершено  
**Время фактическое**: 2 часа  
**Дата завершения**: December 5, 2025

---

## 📋 Этап 2: Утилиты (3-4 часа)

- [x] Создан `app/webapp/js/utils/formatters.js`:
  - [x] formatNumber() - форматирование с разделителями
  - [x] formatCurrency() - форматирование валюты
  - [x] getAgeCategory() - метки возрастных категорий
  - [x] formatEngineVolume() - форматирование объема двигателя
  - [x] formatYear() - форматирование года
  - [x] formatPercent() - форматирование процентов
  - [x] truncateToBytes() - обрезка строк до байтового лимита
  - [x] byteLength() - подсчет байтов в UTF-8

- [x] Создан `app/webapp/js/utils/dom.js`:
  - [x] show() / hide() - управление классом .show
  - [x] setContent() / setText() - установка контента
  - [x] toggle() - переключение видимости
  - [x] setDisplay() - установка display стиля
  - [x] addClass() / removeClass() / hasClass() - работа с классами
  - [x] getEl() - получение элемента по ID
  - [x] query() / queryAll() - селекторы
  - [x] debounce() - отложенное выполнение
  - [x] throttle() - ограничение частоты вызовов
  - [x] createElement() - создание элементов с пропсами
  - [x] clearChildren() - удаление дочерних элементов
  - [x] scrollToElement() - плавная прокрутка

- [x] Создан `tests/manual/test_formatters.html`:
  - [x] Тесты formatNumber (3 cases)
  - [x] Тесты formatCurrency (3 cases)
  - [x] Тесты getAgeCategory (3 cases)
  - [x] Тесты formatEngineVolume (2 cases)
  - [x] Тесты formatYear (2 cases)
  - [x] Тесты formatPercent (1 case)
  - [x] Тесты truncateToBytes (2 cases)
  - [x] Тесты byteLength (2 cases)
  - [x] Тесты DOM утилит (8 cases)

- [x] Обновлен `app/webapp/index.html`:
  - [x] Добавлен type="module" к тегу script
  - [x] Добавлены импорты formatters и dom
  - [x] Заменены локальные функции на импортированные

- [x] Проверка:
  - [x] Открыт test_formatters.html - все тесты проходят
  - [x] WebApp загружается без ошибок модулей
  - [x] Форматирование работает корректно
  - [x] Нет ошибок в консоли

- [x] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 2 - утилиты форматирования и DOM"
  ```

**Статус**: ✅ Завершено  
**Время фактическое**: 2.5 часа  
**Дата завершения**: December 5, 2025

---

## 📋 Этап 3: Конфигурация (2-3 часа)

- [ ] Создан `app/webapp/js/utils/formatters.js`:
  - [ ] `formatNumber(num)`
  - [ ] `formatCurrency(amount, currency)`
  - [ ] `getAgeCategory(category)`
  - [ ] export функций

- [ ] Создан `app/webapp/js/utils/dom.js`:
  - [ ] `show(element)`
  - [ ] `hide(element)`
  - [ ] `setText(elementId, text)`
  - [ ] `setContent(elementId, html)`
  - [ ] `debounce(fn, delay)`
  - [ ] export функций

- [ ] Обновлен `index.html`:
  - [ ] Импортированы утилиты
  - [ ] Заменены вызовы на импортированные функции

- [ ] Проверка:
  - [ ] formatNumber работает (числа форматируются)
  - [ ] show/hide работают (элементы показываются/скрываются)
  - [ ] debounce работает (задержка срабатывает)

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 2 - вынос утилит"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 3: Конфигурация (1-2 часа)

- [x] Создан `app/webapp/js/config/messages.js`:
  - [x] `Messages.errors` (все сообщения об ошибках: NO_COUNTRY, INVALID_YEAR_FUTURE, INVALID_YEAR_OLD, INVALID_ENGINE_RANGE, INVALID_PRICE, CALCULATION_ERROR, etc.)
  - [x] `Messages.buttons` (тексты кнопок: CALCULATE, BACK, SHARE, TAB_CALC, TAB_RESULT)
  - [x] `Messages.labels` (метки полей: COUNTRY, YEAR, ENGINE, PRICE, AGE, TOTAL, etc.)
  - [x] `Messages.breakdown` (компоненты стоимости: PURCHASE_PRICE, DUTIES, FREIGHT, etc.)
  - [x] `Messages.info` (информационные сообщения: COPIED, SENT_TO_CHAT)
  - [x] `Messages.warnings` (предупреждения: NON_M1_DISCLAIMER, LARGE_MESSAGE, WARNING_PREFIX)
  - [x] `Messages.share` (шаблоны для шеринга: TITLE, TITLE_FROM_COUNTRY, BREAKDOWN_TITLE)
  - [x] `Messages.age/freight/vehicle/countries/currencies` (fallback labels)
  - [x] export Messages

- [x] Создан `app/webapp/js/config/constants.js`:
  - [x] `Constraints` (YEAR_MIN=1990, YEAR_MAX, ENGINE_CC_MIN=500, ENGINE_CC_MAX=10000, PRICE_MIN=1)
  - [x] `API_ENDPOINTS` (/api/calculate, /api/meta, /api/rates, /api/health)
  - [x] `API_CONFIG` (RETRY_COUNT=3, TIMEOUT=10000, MAX_PAYLOAD_SIZE=4096)
  - [x] `DEFAULT_VALUES` (COUNTRY='japan', ENGINE_CC=1500, YEAR_OFFSET=3, VEHICLE_TYPE='M1')
  - [x] `COUNTRY_EMOJI` (fruit emojis: japan=🍇, korea=🍊, uae=🍉, china=🍑, georgia=🍒)
  - [x] `FALLBACK_META` (offline данные для /api/meta)
  - [x] `HAPTIC_TYPES, TOAST_CONFIG, ANIMATION, DEBOUNCE` (UI константы)
  - [x] export всех констант

- [x] Обновлен `app/webapp/index.html`:
  - [x] Добавлены импорты Messages и Constants
  - [x] Заменены все хардкод строки на Messages (50+ замен)
  - [x] Заменены все магические числа на Constraints (1990→YEAR_MIN, 500/10000→ENGINE_CC_MIN/MAX, 1500→DEFAULT_VALUES.ENGINE_CC, 800→ANIMATION.TELEGRAM_CLOSE_DELAY)
  - [x] Заменены URL на API_ENDPOINTS (/api/calculate→API_ENDPOINTS.CALCULATE, /api/meta→API_ENDPOINTS.META)
  - [x] Заменены хардкод fallback metadata на FALLBACK_META
  - [x] Заменены типы haptic feedback на HAPTIC_TYPES.LIGHT/MEDIUM/HEAVY
  - [x] Создана функция applyFormConstraints() для динамического применения констант к полям формы

- [x] Синхронизация с бэкендом:
  - [x] Constraints.YEAR_MIN (1990) ↔ models.py @field_validator (year < 1990)
  - [x] Constraints.ENGINE_CC_MIN (500) ↔ models.py Field(gt=0) + бизнес-логика
  - [x] Messages.errors.INVALID_YEAR_OLD ↔ app/core/messages.py ERR_YEAR_TOO_OLD
  - [x] Messages.errors.INVALID_YEAR_FUTURE ↔ app/core/messages.py ERR_YEAR_FUTURE

- [x] Проверка:
  - [x] Нет ошибок в консоли при загрузке
  - [x] Импорты модулей работают
  - [x] Сообщения об ошибках отображаются корректно
  - [x] Валидация использует константы из Constraints
  - [x] API запросы идут на правильные эндпоинты
  - [x] Форма заполняется дефолтными значениями из DEFAULT_VALUES

- [x] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 3 - константы и конфигурация (Single Source of Truth)"
  ```

**Статус**: ✅ Завершено  
**Время фактическое**: 2 часа  
**Дата завершения**: December 5, 2025

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 3 - вынос конфигурации"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 4: Валидация (2-3 часа)

- [x] Создан `app/webapp/js/modules/validator.js`:
  - [x] Класс `FormValidator` с конструктором (принимает constraints)
  - [x] Метод `validate(formData)` → {isValid: boolean, errors: Array}
  - [x] Метод `validateField(name, value)` → string | null
  - [x] Метод `getFieldConstraints(name)` → {min, max, step} | null
  - [x] Метод `addCustomValidator(fieldName, fn)` → this
  - [x] Метод `removeCustomValidator(fieldName)` → boolean
  - [x] Метод `clearCustomValidators()` → void
  - [x] Метод `hasCustomValidator(fieldName)` → boolean
  - [x] Валидация года (YEAR_MIN=1990, YEAR_MAX=current, не в будущем)
  - [x] Валидация объёма двигателя (ENGINE_CC_MIN=500, ENGINE_CC_MAX=10000)
  - [x] Валидация цены (PRICE_MIN=1, больше 0)
  - [x] Валидация страны (не пустое значение)
  - [x] Поддержка FormData и plain object
  - [x] Поддержка camelCase и snake_case (engineCc/engine_cc, purchasePrice/purchase_price)
  - [x] Экспорт createValidator() и default instance validator

- [x] Добавлены стили валидации в `app/webapp/css/components.css`:
  - [x] input.error / select.error (красная рамка, анимация shake)
  - [x] .field-error (inline ошибки с fade-in анимацией)
  - [x] @keyframes shake и fadeIn

- [x] Обновлен `app/webapp/index.html`:
  - [x] Добавлен импорт FormValidator
  - [x] Создан экземпляр `formValidator = new FormValidator()`
  - [x] Заменена функция `validateForm()` на использование `formValidator.validate()`
  - [x] Добавлена функция `getFieldIdFromName()` для маппинга полей
  - [x] Добавлена функция `setupRealTimeValidation()` для real-time валидации
  - [x] Добавлены функции `validateFieldRealTime()`, `showFieldError()`, `clearFieldError()`
  - [x] Подключены event listeners (blur для валидации, input для очистки ошибок)
  - [x] Обработка ValidationResult.errors с подсветкой полей

- [x] Создан `tests/manual/test_validator.html`:
  - [x] 40+ test cases (constructor, year, engine, price, country, full form, constraints, custom validators)
  - [x] Interactive demo form с real-time валидацией
  - [x] Test summary (pass/fail count)
  - [x] Автоматический запуск тестов при загрузке

- [x] Проверка:
  - [x] Валидация года работает (2026 → ошибка "Год выпуска не может быть больше текущего")
  - [x] Валидация года работает (1989 → ошибка "Год выпуска должен быть не менее 1990")
  - [x] Валидация объёма работает (499 → ошибка "Объем двигателя должен быть от 500 до 10000 см³")
  - [x] Валидация цены работает (0 → ошибка "Цена покупки должна быть больше 0")
  - [x] Real-time валидация работает (ошибки показываются при blur, очищаются при input)
  - [x] Кастомные валидаторы работают (add/remove/clear)
  - [x] test_validator.html проходит все тесты

- [x] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 4 - модуль валидации (FormValidator + real-time)"
  ```

**Статус**: ✅ Завершено  
**Время фактическое**: 2.5 часа  
**Дата завершения**: December 5, 2025

---

## 📋 Этап 5: API клиент (2-3 часа)

- [ ] Создан `app/webapp/js/modules/api.js`:
  - [ ] Класс `APIError extends Error`
  - [ ] Класс `APIClient`
  - [ ] Метод `fetchWithTimeout()` (AbortController)
  - [ ] Метод `fetchWithRetry()` (retry логика)
  - [ ] Метод `calculate(request)` → Promise<CalculationResult>
  - [ ] Метод `getMeta()` → Promise<MetaData>
  - [ ] Метод `getRates()` → Promise<RatesData>
  - [ ] export APIClient, APIError

- [ ] Обновлен код:
  - [ ] Создан `api = new APIClient()`
  - [ ] Заменены вызовы `api.post('/api/calculate')` на `api.calculate()`
  - [ ] Обработка `APIError` (status, message)

- [ ] Проверка:
  - [ ] Успешный запрос работает
  - [ ] Timeout срабатывает (имитация медленного API)
  - [ ] Retry работает (имитация сбоя сети)
  - [ ] 4xx/5xx ошибки обрабатываются

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 5 - улучшенный API клиент"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 6: UI модуль (3-4 часа)

- [ ] Создан `app/webapp/js/modules/ui.js`:
  - [ ] Класс `UI`
  - [ ] Метод `showError(message)`
  - [ ] Метод `hideError()`
  - [ ] Метод `showLoading(text?)`
  - [ ] Метод `hideLoading()`
  - [ ] Метод `showResult()`
  - [ ] Метод `hideResult()`
  - [ ] Метод `showShareButton()` / `hideShareButton()`
  - [ ] export UI

- [ ] Обновлен код:
  - [ ] Создан `ui = new UI()`
  - [ ] Заменены функции `showError()` на `ui.showError()`
  - [ ] Заменены функции `showLoading()` на `ui.showLoading()`
  - [ ] Заменены функции `hideResult()` на `ui.hideResult()`

- [ ] Проверка:
  - [ ] Ошибки показываются/скрываются
  - [ ] Спиннер загрузки работает
  - [ ] Карточка результата показывается
  - [ ] Кнопка "Поделиться" показывается

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 6 - UI модуль"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 7: Results renderer (3-4 часа)

- [ ] Создан `app/webapp/js/modules/results.js`:
  - [ ] Класс `ResultsRenderer`
  - [ ] Метод `render(result)`
  - [ ] Метод `renderTotal(breakdown)`
  - [ ] Метод `renderBreakdown(breakdown)`
  - [ ] Метод `renderMeta(meta, request)`
  - [ ] Метод `createBreakdownItem(label, amount, isTotal?)`
  - [ ] Метод `getEngineDisplay(meta, request)`
  - [ ] Метод `addDutyInfo(parts, meta)`
  - [ ] Метод `addWarnings(parts, meta)`
  - [ ] Метод `getCountryLabel(code)`
  - [ ] Метод `getAgeCategory(category)`
  - [ ] export ResultsRenderer

- [ ] Обновлен код:
  - [ ] Создан `resultsRenderer = new ResultsRenderer(metaData)`
  - [ ] Заменена функция `displayResult()` на `resultsRenderer.render()`

- [ ] Проверка:
  - [ ] Итоговая сумма отображается
  - [ ] Breakdown с компонентами отображается
  - [ ] Метаданные (возраст, объём, пошлина) отображаются
  - [ ] Warnings отображаются

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 7 - results renderer"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 8: Calculator controller (3-4 часа)

- [ ] Создан `app/webapp/js/modules/calculator.js`:
  - [ ] Класс `CalculatorController`
  - [ ] Constructor (api, validator, ui, resultsRenderer, telegram)
  - [ ] Свойства: selectedCountry, selectedFreightType
  - [ ] Метод `setCountry(country)`
  - [ ] Метод `setFreightType(freightType)`
  - [ ] Метод `handleSubmit(form)` (главная логика)
  - [ ] Метод `buildRequest(formData)`
  - [ ] Метод `updateTelegramButton()`
  - [ ] export CalculatorController

- [ ] Обновлен код:
  - [ ] Создан `calculator = new CalculatorController(telegram, metaData)`
  - [ ] Заменена функция `calculateCost()` на `calculator.handleSubmit(form)`
  - [ ] Заменена `selectCountry()` на `calculator.setCountry()`
  - [ ] Заменена `updateMainButtonState()` на `calculator.updateTelegramButton()`

- [ ] Проверка:
  - [ ] Форма отправляется и результат показывается
  - [ ] Валидация работает перед отправкой
  - [ ] Ошибки API обрабатываются
  - [ ] Telegram MainButton обновляется

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 8 - calculator controller"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 9: Обновить HTML (2-3 часа)

- [ ] Обновлен `app/webapp/index.html`:
  - [ ] Минимизирован `<script>` блок
  - [ ] Добавлен `type="module"` к script
  - [ ] Импортированы все модули
  - [ ] Создана функция `init()`
  - [ ] Создана функция `setupEventListeners()`
  - [ ] Вызов `init()` при загрузке
  - [ ] Удалены старые глобальные функции

- [ ] Проверка структуры:
  - [ ] HTML < 200 строк
  - [ ] JS логика в модулях
  - [ ] Нет дублирования кода

- [ ] Полное тестирование:
  - [ ] Выбор страны работает
  - [ ] Заполнение формы работает
  - [ ] Валидация срабатывает
  - [ ] Расчёт выполняется
  - [ ] Результат отображается
  - [ ] Telegram кнопки работают
  - [ ] Поделиться работает

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 9 - минимизация HTML"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📋 Этап 10: Базовые тесты (2-3 часа, опционально)

- [ ] Создан `tests/unit/test_formatters.html`:
  - [ ] Тест `formatNumber` с числами
  - [ ] Тест `formatNumber` с null/undefined
  - [ ] Тест `getAgeCategory` с категориями

- [ ] Создан `tests/unit/test_validator.html`:
  - [ ] Тест валидации года (будущий год)
  - [ ] Тест валидации года (< 1990)
  - [ ] Тест валидации объёма (< 500)
  - [ ] Тест валидации объёма (> 10000)
  - [ ] Тест валидации цены (<= 0)
  - [ ] Тест успешной валидации

- [ ] Запуск тестов:
  - [ ] Открыт test_formatters.html - все тесты ✅
  - [ ] Открыт test_validator.html - все тесты ✅

- [ ] Коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): этап 10 - базовые unit-тесты"
  ```

**Статус**: ⬜ Не начато | ⏳ В процессе | ✅ Завершено  
**Время фактическое**: _____ часов

---

## 📊 Финальная проверка

### Код Quality
- [ ] Каждый файл < 300 строк
- [ ] Нет глобальных переменных (кроме telegram, calculator в init)
- [ ] Нет дублирования кода
- [ ] Нет магических чисел/строк
- [ ] Все функции имеют понятные имена
- [ ] Комментарии только где необходимо

### Функциональность
- [ ] Webapp открывается в браузере
- [ ] Webapp работает в Telegram
- [ ] Все страны доступны
- [ ] Все типы фрахта работают
- [ ] Расчёт возвращает правильный результат
- [ ] Результат форматируется корректно
- [ ] Warnings отображаются
- [ ] Поделиться работает

### Performance
- [ ] CSS файлы кэшируются
- [ ] JS модули кэшируются
- [ ] Debounce на input работает
- [ ] Нет зависаний UI

### Документация
- [ ] Обновлён `docs/rpg.yaml` (new components)
- [ ] Создан CHANGELOG.md с изменениями
- [ ] README обновлён (если нужно)

---

## 🎯 Метрики успеха

**Проверь что цели достигнуты**:

| Метрика | Цель | Фактическое | ✅/❌ |
|---------|------|-------------|------|
| Строк в самом большом файле | < 300 | _____ | |
| Время добавления новой страны | < 30 мин | _____ | |
| Количество мест с валидацией | 1 | _____ | |
| Количество мест с Messages | 1 | _____ | |
| Браузерное кэширование CSS/JS | Да | _____ | |
| Время понимания кода новичком | < 30 мин | _____ | |

---

## 📝 Заметки и проблемы

**Проблемы, с которыми столкнулись**:
```
(Записывайте здесь проблемы и их решения)

1. 
2. 
3. 
```

**Улучшения, которые можно сделать в будущем**:
```
(Идеи для следующих итераций)

1. 
2. 
3. 
```

---

## 🎉 Завершение

- [ ] Все этапы завершены
- [ ] Все тесты пройдены
- [ ] Документация обновлена
- [ ] Финальный коммит:
  ```bash
  git add .
  git commit -m "refactor(webapp): завершён рефакторинг - модульная структура"
  git push
  ```

**Дата завершения**: _____________  
**Общее время**: _____ часов  
**Статус**: 🎉 ЗАВЕРШЕНО

---

**Следующие шаги**:
1. Мониторинг production в течение недели
2. Сбор обратной связи от команды
3. Планирование следующих улучшений

