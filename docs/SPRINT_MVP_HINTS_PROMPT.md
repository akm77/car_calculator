# 🎯 ПРОМПТ: Реализация MVP системы визуальных подсказок (Вариант 3 упрощённый)

**Дата создания:** 2025-12-08  
**Спринт:** MVP UI Hints  
**Методология:** Repository Planning Graph (RPG)  
**Время оценка:** 2-3 часа

---

## 👤 РОЛЬ МОДЕЛИ

Ты — **Фронтенд разработчик-эксперт** с глубоким знанием:
- Vanilla JavaScript (ES6+)
- CSS3 (анимации, переходы, адаптивная вёрстка)
- Доступность (WCAG 2.1, ARIA атрибуты)
- UX паттернов для подсказок и валидации
- Модульной архитектуры фронтенда

Ты работаешь с проектом **car_calculator** — калькулятором стоимости ввоза автомобилей, где критически важно помочь пользователям выбрать оптимальные параметры авто (возраст, мощность) для минимизации таможенных платежей.

---

## 📐 МЕТОДОЛОГИЯ RPG

Ты работаешь по технике **Repository Planning Graph (RPG)** (см. `docs/rpg_intro.txt`):

### Принципы работы:
1. **Структурированный граф планирования** вместо расплывчатых описаний
2. **Топологический порядок реализации** — зависимости раньше зависящих
3. **Инкрементальная валидация** — тестируй каждый компонент
4. **Стабильные интерфейсы** — фиксируй API до реализации
5. **Модульность** — группируй связанные функции, разделяй несвязанные

### Обязательные действия по RPG:
- ✅ **После завершения работ обновить граф проекта** в `docs/rpg.yaml`:
  - Добавь новый узел `app_webapp_hints` в секцию `modules`
  - Обнови `metadata.recent_changes` с описанием изменений
  - Обнови `metadata.last_updated` на текущую дату (2025-12-08)
  - Укажи количество добавленных строк кода

---

## 🎯 ЦЕЛИ РАБОТЫ

### Главная цель:
**Интегрировать визуальные подсказки в форму калькулятора**, чтобы пользователи СРАЗУ видели оптимальность выбранных параметров автомобиля БЕЗ перегрузки интерфейса.

### Конкретные задачи:

#### 1. Цветовая индикация критичных полей ✅
- **Поля:** "Год выпуска" и "Мощность двигателя (л.с.)"
- **Зоны:**
  - 🟢 **Зелёный (optimal)** — оптимальный выбор (минимальные платежи)
  - 🔵 **Голубой (acceptable)** — приемлемый выбор
  - 🟠 **Оранжевый (warning)** — осторожно, платежи выше
  - 🔴 **Красный (prohibitive)** — невыгодно/запретительно

#### 2. Краткие текстовые подсказки ✅
- Появляются **под полем при вводе значения**
- Краткий текст (1 строка) с объяснением зоны
- **Обязательно показывать для красной зоны** с предупреждением

#### 3. Информационная иконка ℹ️ ✅
- Рядом с label каждого критичного поля
- При клике — **простой tooltip** с кратким объяснением порогов
- Использовать нативный HTML `title` или CSS-based tooltip
- Текст tooltip'а должен быть информативным и кратким

---

## 📚 ИСТОЧНИКИ ПРАВДЫ

### Первичные источники (PRIMARY):

1. **`docs/QUICK_REFERENCE_AGE_OPTIMAL.md`**
   - Оптимальный возраст: **3-5 лет**
   - Зоны: ≤3 лет (дорого), 3-5 лет (оптимально), >5 лет (дорого)
   - Граница критична: разница в 1 год = экономия 50-80%

2. **`docs/QUICK_REFERENCE_POWER_LIMITS.md`**
   - Критический порог: **200 л.с. (147 кВт)**
   - Зоны: 
     - 🟢 до 160 л.с. — безопасно
     - 🟡 160-200 л.с. — начинается рост
     - 🟠 200-300 л.с. — высокий риск
     - 🔴 >300 л.с. — запретительно

3. **`docs/UTILIZATION_ANALYSIS.md`**
   - Детальные коэффициенты утилизационного сбора
   - Таблицы по мощности и объёму двигателя
   - Реальные примеры автомобилей

4. **`docs/AGE_ANALYSIS.md`**
   - Детальный анализ пошлин по возрасту
   - Формулы расчёта для каждой категории
   - Примеры экономии

5. **`docs/UI_HINTS_INTEGRATION_PROPOSAL.md`**
   - Полное описание концепции подсказок
   - **Вариант 3 (комбинированный)** — рекомендуемый подход
   - **MVP (упрощённый)** — раздел "🚀 MVP для быстрого старта (2 часа)"
   - Цветовая схема, CSS стили, JavaScript логика
   - Примеры кода и разметки

6. **`docs/UI_HINTS_SUMMARY.md`**
   - Краткое резюме подхода
   - Цветовая схема и примеры текстов
   - План внедрения

### Вторичные источники (REFERENCE):

7. **`app/webapp/index.html`**
   - Текущая структура формы
   - Существующие поля и их ID
   - JavaScript модули (api.js, validator.js, formatters.js и т.д.)

8. **`app/webapp/js/config/messages.js`**
   - Существующие тексты интерфейса
   - Место для добавления текстов подсказок

9. **`app/webapp/js/config/constants.js`**
   - Валидационные константы (ENGINE_POWER_HP_MIN/MAX и т.д.)
   - Место для добавления порогов подсказок

10. **`app/webapp/css/`**
    - Существующие стили (variables.css, components.css)
    - CSS модульная структура

---

## ✅ КРИТЕРИИ ДОСТИЖЕНИЯ РЕЗУЛЬТАТА

### MUST HAVE (Обязательно):

#### 1. Цветовая индикация работает ✅
- [ ] Поле "Год выпуска" меняет цвет рамки в зависимости от возраста
- [ ] Поле "Мощность двигателя (л.с.)" меняет цвет рамки
- [ ] 4 цветовые зоны работают корректно (зелёный/голубой/оранжевый/красный)
- [ ] Изменение цвета происходит в real-time при вводе

#### 2. Текстовые подсказки отображаются ✅
- [ ] Краткий текст появляется под полем после ввода значения
- [ ] Текст соответствует зоне (см. `docs/UI_HINTS_SUMMARY.md`)
- [ ] Для красной зоны текст содержит явное предупреждение
- [ ] Подсказки исчезают при очистке поля

#### 3. Информационные иконки ℹ️ работают ✅
- [ ] Иконка размещена рядом с label каждого критичного поля
- [ ] При клике/наведении показывается tooltip с объяснением порогов
- [ ] Tooltip содержит информативный текст (не перегруженный)
- [ ] Tooltip корректно работает на мобильных устройствах

#### 4. Код соответствует архитектуре проекта ✅
- [ ] Новый CSS вынесен в отдельный файл `css/hints.css`
- [ ] JavaScript логика в модуле `js/modules/hints.js`
- [ ] Конфигурация порогов в `js/config/constants.js`
- [ ] Тексты подсказок в `js/config/messages.js`
- [ ] Импорты корректно добавлены в `index.html`

#### 5. Accessibility (a11y) обеспечена ✅
- [ ] Использованы ARIA атрибуты (`aria-describedby`, `aria-label`)
- [ ] Цвета дублируются текстом (для дальтоников)
- [ ] Tooltip доступен с клавиатуры (focus + Enter/Space)
- [ ] Контрастность цветов соответствует WCAG 2.1 AA

### SHOULD HAVE (Желательно):

#### 6. UX оптимизирован ⭐
- [ ] Debounce на обработчиках ввода (300ms) для производительности
- [ ] Smooth transitions между цветами (CSS transitions)
- [ ] Подсказки не мешают заполнению формы
- [ ] Tooltip не перекрывает важные элементы

#### 7. Мобильная адаптация ⭐
- [ ] Иконка ℹ️ имеет достаточную зону нажатия (44×44px)
- [ ] Tooltip корректно позиционируется на малых экранах
- [ ] Цветовая индикация хорошо видна на мобильных

#### 8. Тестирование ⭐
- [ ] Создан мануальный тест `tests/manual/test_hints_mvp.html`
- [ ] Проверены граничные значения (160 л.с., 200 л.с., возраст 3/5 лет)
- [ ] Протестировано на Chrome, Firefox, Safari
- [ ] Протестировано в Telegram WebApp

### NICE TO HAVE (Опционально):

#### 9. Дополнительные улучшения 💎
- [ ] Haptic feedback при попадании в красную зону (Telegram API)
- [ ] Анимация появления подсказок (fade-in)
- [ ] Возможность отключить подсказки (настройка)
- [ ] Логирование взаимодействий для аналитики

---

## 🚫 ИЗБЕГАЙ ОВЕРИНЖИНИРИНГА

### ❌ НЕ ДЕЛАЙ:

1. **Не создавай сложные модальные окна или drawer'ы**
   - Это MVP, достаточно простых tooltip'ов
   - Drawer'ы — для следующего этапа

2. **Не добавляй API endpoints для подсказок**
   - Вся логика на фронтенде (пороги статичны)
   - Не усложняй архитектуру

3. **Не пиши тесты для каждой функции**
   - Создай один мануальный HTML тест для проверки
   - Unit-тесты — для следующего этапа

4. **Не анимируй каждое изменение**
   - Простые CSS transitions достаточно
   - Сложные анимации замедлят работу

5. **Не обрабатывай все поля формы**
   - **ТОЛЬКО** "Год выпуска" и "Мощность двигателя (л.с.)"
   - Остальные поля — для будущих итераций

6. **Не меняй существующую логику валидации**
   - Подсказки ≠ валидация
   - Они работают параллельно, не заменяя друг друга

---

## 📋 ТОПОЛОГИЧЕСКИЙ ПОРЯДОК РЕАЛИЗАЦИИ

### Этап 1: Конфигурация (30 минут)

**Зависимости:** нет  
**Файлы:** `js/config/constants.js`, `js/config/messages.js`

**Задачи:**
1. Добавь в `constants.js` объект `HINT_THRESHOLDS`:
   ```javascript
   HINT_THRESHOLDS: {
     AGE: {
       OPTIMAL_MIN: 3,
       OPTIMAL_MAX: 5
     },
     POWER: {
       OPTIMAL_MAX: 160,
       ACCEPTABLE_MAX: 200,
       WARNING_MAX: 300
     }
   }
   ```

2. Добавь в `messages.js` объект `hints`:
   ```javascript
   hints: {
     age: {
       optimal: "🟢 Оптимальный возраст для минимальной пошлины",
       new_expensive: "🔴 Высокая пошлина для дорогих авто",
       old_expensive: "🟠 Пошлина выше, чем для авто 3-5 лет",
       tooltip: "Критический порог: 3-5 лет. В этом диапазоне пошлины минимальны."
     },
     power: {
       optimal: "🟢 Минимальный утилизационный сбор (5,200 ₽)",
       acceptable: "🔵 Утильсбор начинает расти",
       warning: "🟠 Высокий утилизационный сбор",
       prohibitive: "🔴 Запретительный утильсбор! При 200+ л.с. сбор может превысить цену авто",
       tooltip: "Критический порог: 200 л.с. После этой мощности утильсбор резко возрастает."
     }
   }
   ```

---

### Этап 2: CSS стили (30 минут)

**Зависимости:** нет  
**Файлы:** `css/hints.css` (новый)

**Задачи:**
1. Создай файл `css/hints.css`
2. Определи классы для цветовой индикации:
   - `.input-hint-optimal` (зелёный)
   - `.input-hint-acceptable` (голубой)
   - `.input-hint-warning` (оранжевый)
   - `.input-hint-prohibitive` (красный)
3. Стили для текстовых подсказок под полем (`.hint-text`)
4. Стили для иконки ℹ️ (`.info-icon`)
5. Стили для tooltip'а (`.hint-tooltip`)
6. CSS transitions для плавного изменения цветов
7. Адаптивность для мобильных

**Референс:** `docs/UI_HINTS_INTEGRATION_PROPOSAL.md` (раздел "Цветовая схема")

---

### Этап 3: JavaScript модуль подсказок (60 минут)

**Зависимости:** Этап 1 (конфигурация)  
**Файлы:** `js/modules/hints.js` (новый)

**Задачи:**
1. Создай класс `HintsManager`:
   ```javascript
   class HintsManager {
     constructor(constants, messages) { }
     
     // Определить зону для возраста
     getAgeHintData(year) { }
     
     // Определить зону для мощности
     getPowerHintData(powerHp) { }
     
     // Применить визуальную индикацию к полю
     applyHintToField(fieldElement, hintData) { }
     
     // Показать/скрыть текстовую подсказку
     showHintText(fieldElement, text) { }
     hideHintText(fieldElement) { }
     
     // Инициализировать tooltip'ы
     initTooltips() { }
   }
   ```

2. Реализуй методы `getAgeHintData` и `getPowerHintData`:
   - Используй пороги из `HINT_THRESHOLDS`
   - Возвращай объект: `{ zone: 'optimal', className: '...', text: '...' }`

3. Реализуй `applyHintToField`:
   - Удали старые hint классы
   - Добавь новый className
   - Обнови ARIA атрибуты

4. Реализуй `showHintText` / `hideHintText`:
   - Создай/удали элемент `.hint-text` под полем
   - Используй `insertAdjacentElement`

5. Реализуй `initTooltips`:
   - Добавь иконки ℹ️ рядом с label'ами
   - Привяжи обработчики (click или hover)
   - Создай простой tooltip элемент

---

### Этап 4: Интеграция в форму (30 минут)

**Зависимости:** Этап 2, 3 (CSS, JS модуль)  
**Файлы:** `index.html`

**Задачи:**
1. Добавь импорт `css/hints.css` в `<head>`
2. Добавь импорт `js/modules/hints.js` в секцию скриптов
3. Инициализируй `HintsManager`:
   ```javascript
   const hintsManager = new HintsManager(constants, messages);
   hintsManager.initTooltips();
   ```
4. Добавь обработчики на поля "year" и "enginePowerHp":
   ```javascript
   yearField.addEventListener('input', debounce((e) => {
     const hintData = hintsManager.getAgeHintData(e.target.value);
     hintsManager.applyHintToField(yearField, hintData);
     hintsManager.showHintText(yearField, hintData.text);
   }, 300));
   ```
5. Аналогично для `enginePowerHp`

---

### Этап 5: Тестирование (30 минут)

**Зависимости:** Этап 4 (интеграция)  
**Файлы:** `tests/manual/test_hints_mvp.html` (новый)

**Задачи:**
1. Создай standalone HTML файл для тестирования
2. Включи форму с критичными полями
3. Добавь кнопки для автозаполнения граничных значений:
   - Год: 2022 (новое), 2021 (оптимально), 2018 (старое)
   - Мощность: 150 л.с. (ок), 180 л.с. (граница), 250 л.с. (плохо)
4. Визуальная проверка:
   - Цвета меняются корректно
   - Тексты подсказок появляются
   - Tooltip'ы работают
5. Проверь в разных браузерах
6. Проверь на мобильном (Telegram WebApp или эмулятор)

---

### Этап 6: Документация (20 минут)

**Зависимости:** Этап 5 (тестирование пройдено)  
**Файлы:** `docs/rpg.yaml`, `README.md` (опционально)

**Задачи:**
1. **Обнови `docs/rpg.yaml`:**
   - Добавь узел `app_webapp_hints`:
     ```yaml
     - name: "app_webapp_hints"
       path: "app/webapp/css/hints.css + app/webapp/js/modules/hints.js"
       description: "Визуальные подсказки для критичных полей формы (возраст, мощность)"
       dependencies: ["app_webapp"]
       components:
         - "hints.css (цветовая индикация, tooltip стили)"
         - "hints.js (HintsManager класс)"
         - "Интеграция в index.html"
       features:
         - "Цветовые зоны: optimal, acceptable, warning, prohibitive"
         - "Краткие текстовые подсказки под полями"
         - "Информационные иконки ℹ️ с tooltip'ами"
         - "Real-time обновление при вводе (debounced)"
         - "Accessibility (ARIA, keyboard support)"
       files_changed:
         - "app/webapp/css/hints.css (NEW)"
         - "app/webapp/js/modules/hints.js (NEW)"
         - "app/webapp/js/config/constants.js (MODIFIED)"
         - "app/webapp/js/config/messages.js (MODIFIED)"
         - "app/webapp/index.html (MODIFIED)"
         - "tests/manual/test_hints_mvp.html (NEW)"
       lines_of_code: ~300
       time_spent: "2-3 часа"
       implemented_date: "2025-12-08"
       reference_docs:
         - "docs/UI_HINTS_INTEGRATION_PROPOSAL.md"
         - "docs/UI_HINTS_SUMMARY.md"
         - "docs/QUICK_REFERENCE_AGE_OPTIMAL.md"
         - "docs/QUICK_REFERENCE_POWER_LIMITS.md"
     ```

   - Обнови `metadata.recent_changes`:
     ```yaml
     - date: "2025-12-08"
       description: "MVP UI HINTS COMPLETED: Реализована система визуальных подсказок для критичных полей формы (возраст, мощность) - цветовая индикация (4 зоны: optimal/acceptable/warning/prohibitive), краткие текстовые подсказки под полями, информационные иконки ℹ️ с tooltip'ами, real-time обновление при вводе (debounced), ARIA accessibility, создан hints.css и hints.js модуль, обновлены constants.js и messages.js, создан test_hints_mvp.html ✅"
     ```

   - Обнови `metadata.last_updated`: "2025-12-08"
   - Обнови `metadata.total_lines_of_code`: +300

2. (Опционально) Добавь секцию в README.md о новой функциональности

---

## 🎨 РЕФЕРЕНСЫ КОДА

### Пример: Определение зоны для мощности

```javascript
getPowerHintData(powerHp) {
  const power = parseInt(powerHp);
  if (isNaN(power)) return null;
  
  const { POWER } = this.constants.HINT_THRESHOLDS;
  const { power: messages } = this.messages.hints;
  
  if (power <= POWER.OPTIMAL_MAX) {
    return {
      zone: 'optimal',
      className: 'input-hint-optimal',
      text: messages.optimal
    };
  }
  
  if (power <= POWER.ACCEPTABLE_MAX) {
    return {
      zone: 'acceptable',
      className: 'input-hint-acceptable',
      text: messages.acceptable
    };
  }
  
  if (power <= POWER.WARNING_MAX) {
    return {
      zone: 'warning',
      className: 'input-hint-warning',
      text: messages.warning
    };
  }
  
  return {
    zone: 'prohibitive',
    className: 'input-hint-prohibitive',
    text: messages.prohibitive
  };
}
```

### Пример: CSS для цветовой индикации

```css
/* Зелёный - оптимально */
.input-hint-optimal {
  border: 2px solid #27ae60;
  background: rgba(39, 174, 96, 0.05);
  transition: all 0.3s ease;
}

/* Голубой - приемлемо */
.input-hint-acceptable {
  border: 2px solid #3498db;
  background: rgba(52, 152, 219, 0.05);
  transition: all 0.3s ease;
}

/* Оранжевый - осторожно */
.input-hint-warning {
  border: 2px solid #f39c12;
  background: rgba(243, 156, 18, 0.05);
  transition: all 0.3s ease;
}

/* Красный - запретительно */
.input-hint-prohibitive {
  border: 2px solid #e74c3c;
  background: rgba(231, 76, 60, 0.05);
  transition: all 0.3s ease;
}
```

### Пример: Текстовая подсказка под полем

```javascript
showHintText(fieldElement, text) {
  // Удалить старую подсказку
  this.hideHintText(fieldElement);
  
  // Создать новую
  const hintText = document.createElement('div');
  hintText.className = 'hint-text';
  hintText.textContent = text;
  hintText.setAttribute('role', 'status');
  hintText.setAttribute('aria-live', 'polite');
  
  // Вставить после поля
  fieldElement.insertAdjacentElement('afterend', hintText);
}

hideHintText(fieldElement) {
  const existingHint = fieldElement.nextElementSibling;
  if (existingHint && existingHint.classList.contains('hint-text')) {
    existingHint.remove();
  }
}
```

---

## 🔍 ВАЛИДАЦИЯ РЕЗУЛЬТАТА

### Чек-лист перед завершением:

#### Функциональность:
- [ ] Открой `/web/` в браузере
- [ ] Введи год выпуска: 2021 → должен быть зелёный
- [ ] Введи год выпуска: 2023 → должен быть красный
- [ ] Введи мощность: 150 л.с. → должен быть зелёный
- [ ] Введи мощность: 250 л.с. → должен быть красный с предупреждением
- [ ] Кликни на ℹ️ → должен появиться tooltip
- [ ] Очисти поле → подсказка должна исчезнуть

#### Качество кода:
- [ ] Нет console.error в браузере
- [ ] CSS валиден (проверь в DevTools)
- [ ] Все функции имеют docstrings
- [ ] Код читаемый, переменные названы понятно
- [ ] Нет дублирования кода (DRY principle)

#### Документация:
- [ ] `docs/rpg.yaml` обновлён
- [ ] Граф проекта отражает новые файлы
- [ ] `metadata.recent_changes` содержит запись о спринте

---

## ⚠️ ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ MVP

Этот MVP НЕ включает:
- ❌ Детальные drawer'ы с графиками (следующий этап)
- ❌ API endpoints для подсказок (не нужны)
- ❌ Unit-тесты JavaScript (мануальных достаточно)
- ❌ Подсказки для других полей (только возраст + мощность)
- ❌ Настройка отключения подсказок (добавим позже)
- ❌ Аналитику кликов на ℹ️ (добавим позже)

Это намеренно упрощённая версия для быстрой валидации концепции!

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Запустить приложение локально
poetry run python -m app.main

# Или через скрипт
./scripts/run.sh

# Открыть в браузере
open http://localhost:8000/web/

# Проверить Telegram WebApp
# (нужен настроенный бот с WebApp URL)
```

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения этого спринта пользователь должен:

1. **Видеть сразу при вводе**, что его выбор оптимален или нет (цвет рамки)
2. **Получать краткое объяснение** (текст под полем)
3. **Иметь возможность узнать детали** (клик на ℹ️)
4. **Принимать осознанное решение** о выборе параметров авто

### Метрики успеха:
- ✅ Визуальные подсказки работают в 100% случаев
- ✅ Интерфейс остаётся чистым и не перегруженным
- ✅ Время реализации ≤ 3 часа
- ✅ Нет багов на основных браузерах (Chrome, Firefox, Safari)
- ✅ Граф проекта актуален

---

## 🚀 НАЧИНАЙ РАБОТУ!

Ты — эксперт, ты знаешь что делать. Следуй топологическому порядку, тестируй каждый компонент, обновляй граф проекта. 

**Принцип RPG:** Реализуй зависимости раньше зависящих, валидируй инкрементально, не коммить до прохождения проверки.

**Удачи! 🎉**

