# API Client Testing Guide

## Быстрый старт

### 1. Запустить сервер
```bash
cd /Users/admin/PycharmProjects/car_calculator
python -m app.main
```

### 2. Открыть тест в браузере
```
http://localhost:8000/tests/manual/test_api_client.html
```

---

## Тесты

### ✅ Автоматические тесты (без настройки)

#### 1️⃣ Basic GET Request
- Кнопка: "Запустить тест"
- Ожидается: ✅ Успешная загрузка метаданных
- Проверяет: `api.getMeta()`

#### 2️⃣ Basic POST Request
- Кнопка: "Запустить тест"
- Ожидается: ✅ Успешный расчёт стоимости
- Проверяет: `api.calculate(formData)`

#### 3️⃣ Validation Error
- Кнопка: "Запустить тест"
- Ожидается: ✅ Ошибка валидации с понятным сообщением
- Проверяет: APIError.ValidationError, getUserMessage()

#### 4️⃣ Network Error
- Кнопка: "Запустить тест"
- Ожидается: ✅ Обработка 404 ошибки
- Проверяет: Несуществующий эндпоинт

#### 7️⃣ API Methods
- Кнопки: "getMeta()", "getRates()", "refreshRates()"
- Ожидается: ✅ Все методы работают
- Проверяет: Специфичные методы API

#### 8️⃣ Error Types
- Кнопка: "Запустить тест"
- Ожидается: ✅ Все методы APIError работают
- Проверяет: isNetworkError(), isTimeoutError(), etc.

---

### ⚙️ Тесты с настройкой

#### 5️⃣ Timeout Test
**Требует настройки DevTools**

1. Открыть DevTools (F12 или Cmd+Opt+I)
2. Перейти в Network tab
3. Включить Throttling → **Slow 3G**
4. Нажать "Запустить тест (короткий timeout)"

**Ожидается**: ✅ TimeoutError через 100ms

**Альтернатива** (если сервер быстрый):
- Сервер может ответить быстрее 100ms
- В этом случае увидите: ⚠️ "Запрос прошел быстрее timeout"
- Это тоже нормально - значит сервер работает очень быстро

#### 6️⃣ Retry Test
**Требует ручного прерывания сети**

1. Нажать "Запустить тест"
2. **Сразу** отключить Wi-Fi на 2-3 секунды
3. Включить Wi-Fi обратно

**Что смотреть**:
- В DevTools Console должны появиться логи:
  ```
  [APIClient] Attempt 1 failed: Network error. Retrying in 1000ms...
  [APIClient] Attempt 2 failed: Network error. Retrying in 2000ms...
  [APIClient] Request succeeded on attempt 3
  ```

**Если не получилось**:
- Попробуйте использовать DevTools → Network → Offline
- Или включите Throttling → Offline

---

## Проверка конфигурации

В верхней части страницы отображается текущая конфигурация:

```
Текущая конфигурация API_CONFIG:
RETRY_COUNT: 3 | RETRY_DELAY: 1000ms | TIMEOUT: 10000ms
Base URL: http://localhost:8000
```

Эти значения берутся из `app/webapp/js/config/constants.js`:
```javascript
export const API_CONFIG = {
    RETRY_COUNT: 3,        // Максимум попыток
    RETRY_DELAY: 1000,     // Начальная задержка (exponential)
    TIMEOUT: 10000,        // Timeout в ms
};
```

---

## Интерпретация результатов

### ✅ Зелёный блок (Success)
```
✅ Успешно!

Ответ:
{
  "countries": [...],
  ...
}
```
Тест прошёл успешно.

### 🔴 Красный блок (Error)
```
❌ Ошибка: Request timeout after 100ms

{
  "name": "APIError",
  "code": "TimeoutError",
  "status": 408,
  ...
}
```
Ожидаемая ошибка - тест проверяет обработку ошибок.

### 🟡 Жёлтый блок (Loading)
```
⏳ Выполнение запроса...
```
Запрос в процессе выполнения.

---

## DevTools Console

Откройте Console для детального логирования:

### Успешный запрос
```
[APIClient] Initialized with baseURL: http://localhost:8000, timeout: 10000ms, maxRetries: 3
[APIClient] GET http://localhost:8000/api/meta
```

### Retry логика
```
[APIClient] POST http://localhost:8000/api/calculate
[APIClient] Attempt 1 failed: Network error. Retrying in 1000ms...
[APIClient] Attempt 2 failed: Network error. Retrying in 2000ms...
[APIClient] Request succeeded on attempt 3
```

### Ошибка
```
[APIClient] POST /api/calculate failed: {
  "name": "APIError",
  "message": "year: Year must be >= 1990",
  "code": "ValidationError",
  "status": 422,
  "timestamp": "2025-12-05T12:34:56.789Z"
}
```

---

## Network Tab

Откройте Network tab для просмотра HTTP запросов:

### Успешный запрос
- Status: 200 OK
- Method: GET / POST
- URL: /api/meta, /api/calculate
- Response: JSON данные

### Retry (сетевая ошибка)
- Вы увидите несколько одинаковых запросов (3-4 штуки)
- Первые с ошибкой (failed, canceled)
- Последний успешный (200 OK)

### Timeout
- Request canceled (если timeout сработал)
- Timing: Request duration = timeout value

---

## Troubleshooting

### Проблема: Все тесты падают с Network Error
**Причина**: Сервер не запущен  
**Решение**: `python -m app.main`

### Проблема: Timeout тест проходит слишком быстро
**Причина**: Сервер отвечает быстрее 100ms  
**Решение**: Включить Throttling → Slow 3G в DevTools

### Проблема: Retry тест не показывает логи
**Причина**: Сеть не прерывалась во время запроса  
**Решение**: Отключить Wi-Fi **сразу** после нажатия кнопки

### Проблема: 404 на /tests/manual/test_api_client.html
**Причина**: Файл не раздаётся через static  
**Решение**: Проверить, что `app/main.py` раздаёт `/tests/` как static

---

## Дополнительные тесты в DevTools Console

### Тест custom timeout
```javascript
const shortApi = new APIClient({ timeout: 100 });
shortApi.getMeta().catch(err => console.log(err.getUserMessage()));
// Ожидается: "Превышено время ожидания. Попробуйте еще раз."
```

### Тест custom retry count
```javascript
const noRetryApi = new APIClient({ maxRetries: 1 });
// Только 1 попытка вместо 3
```

### Тест error types
```javascript
const err = new APIError('Test', 422, 'ValidationError');
console.log(err.isValidationError()); // true
console.log(err.getUserMessage()); // "Test"
console.log(err.toLogFormat()); // {name, message, code, status, ...}
```

---

## Проверка в Production

### Используйте реальный API
Добавьте query parameter:
```
http://localhost:8000/tests/manual/test_api_client.html?api_base=https://your-production-api.com
```

### Или замените в коде
```javascript
const customApi = new APIClient({
    baseURL: 'https://your-production-api.com'
});
customApi.getMeta();
```

---

## Summary

✅ **8 тестов** покрывают все сценарии  
✅ **Автоматические тесты** (6 из 8) работают без настройки  
✅ **DevTools Console** показывает детальные логи  
✅ **Network Tab** показывает retry и timeout  
✅ **Interactive UI** с цветовой индикацией результатов  

Enjoy testing! 🚀

