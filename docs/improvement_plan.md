# План улучшения проекта Car Calculator

*Дата анализа: 4 декабря 2025*

## Общая оценка проекта

Проект представляет собой хорошо структурированное приложение для расчёта стоимости растаможки автомобилей с использованием современного стека технологий. Общее количество строк кода: ~1690 (app + tests).

### Сильные стороны
- ✅ Чёткая модульная архитектура
- ✅ Использование Pydantic v2 для валидации данных
- ✅ Структурированное логирование (structlog)
- ✅ Наличие функциональных тестов
- ✅ Docker и docker-compose для развертывания
- ✅ Poetry для управления зависимостями
- ✅ PWA WebApp с офлайн-поддержкой
- ✅ Интеграция с Telegram Bot API
- ✅ Разделение конфигураций в YAML

---

## Критические проблемы

### 1. ❌ Падающий тест `test_calculation_cases[gt5_large_engine]`
**Приоритет: КРИТИЧЕСКИЙ**

**Проблема:**
```
Mismatch customs_services_rub: expected 120000.0, got 150000.0
```

Тестовый кейс ожидает 120,000 руб. за таможенные услуги для ОАЭ, но получает 150,000 руб.

**Причина:**
- Несоответствие между тестовыми данными (`tests/test_data/cases.yml`) и конфигурацией (`config/rates.yml`)
- Возможно, тарифы обновились, но тест не был актуализирован

**Решение:**
1. Проверить актуальные тарифы таможенных услуг для ОАЭ
2. Обновить либо `config/rates.yml`, либо `tests/test_data/cases.yml`
3. Добавить комментарии с датой последнего обновления тарифов

---

## Высокоприоритетные улучшения

### 2. 📊 Отсутствие юнит-тестов для критических компонентов
**Приоритет: ВЫСОКИЙ**

**Проблема:**
Отсутствуют юнит-тесты для:
- `CalculationRequest.validate_year` — валидация года выпуска
- `round_rub / to_decimal` — функции округления
- `tariff_tables` core helpers — поиск ставок пошлин
- `rate_limit_middleware` — защита от DoS

**Решение:**
Создать файл `tests/unit/test_rounding.py`:
```python
def test_round_rub_positive():
    assert round_rub(Decimal("12345.67")) == 12346
    
def test_round_rub_negative():
    assert round_rub(Decimal("-12345.67")) == -12346
```

Создать файл `tests/unit/test_models.py`:
```python
def test_validate_year_future():
    with pytest.raises(ValueError):
        CalculationRequest(year=2030, ...)
        
def test_validate_year_too_old():
    with pytest.raises(ValueError):
        CalculationRequest(year=1989, ...)
```

### 3. 🔒 Слабая защита от атак
**Приоритет: ВЫСОКИЙ**

**Проблемы:**
1. Rate limiting реализован в памяти — не работает при горизонтальном масштабировании
2. Нет HTTPS-редиректа (только в nginx-темплейтах)
3. Отсутствует CSRF-защита для критических эндпоинтов
4. Нет авторизации для `/api/rates/refresh`

**Решение:**
```python
# Использовать Redis для rate limiting
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address, storage_uri="redis://localhost:6379")

@router.post("/rates/refresh")
@limiter.limit("1/hour")  # Ограничить до 1 раза в час
async def refresh_rates(request: Request):
    ...
```

Добавить API key для критических операций:
```python
async def verify_api_key(x_api_key: str = Header(...)):
    if x_api_key != settings.admin_api_key:
        raise HTTPException(403)
```

### 4. 🐛 Хрупкая логика округления валют
**Приоритет: ВЫСОКИЙ**

**Проблема:**
В `engine.py` используется `quantize4()` для всех валют, но JPY не имеет дробных частей.

**Текущий код:**
```python
def _convert(amount: Decimal, currency: str, rates_conf: dict) -> Decimal:
    return quantize4(amount * _currency_rate(rates_conf, currency))
```

**Решение:**
```python
def _convert(amount: Decimal, currency: str, rates_conf: dict) -> Decimal:
    result = amount * _currency_rate(rates_conf, currency)
    # JPY не имеет дробных частей
    if currency.upper() == "JPY":
        return result.quantize(Decimal("1"), rounding=ROUND_HALF_UP)
    return quantize4(result)
```

### 5. 📝 Отсутствие документации API (OpenAPI/Swagger)
**Приоритет: ВЫСОКИЙ**

**Проблема:**
FastAPI генерирует документацию автоматически, но:
- Нет описаний для параметров запросов
- Отсутствуют примеры (examples) в моделях Pydantic
- Нет тегов для группировки эндпоинтов

**Решение:**
```python
class CalculationRequest(BaseModel):
    country: Country = Field(
        description="Страна покупки автомобиля",
        examples=["japan"]
    )
    year: int = Field(
        gt=1990,
        lt=2030,
        description="Год выпуска автомобиля",
        examples=[2021]
    )
    # ...

router = APIRouter(
    prefix="/api",
    tags=["Calculations"]
)
```

---

## Среднеприоритетные улучшения

### 6. 🔄 Отсутствие кэширования результатов расчёта
**Приоритет: СРЕДНИЙ**

**Проблема:**
Одинаковые запросы пересчитываются каждый раз, создавая нагрузку на CPU.

**Решение:**
```python
from functools import lru_cache
import hashlib

def _request_hash(req: CalculationRequest) -> str:
    return hashlib.md5(req.model_dump_json().encode()).hexdigest()

# Простой кэш на 1000 последних расчётов
@lru_cache(maxsize=1000)
def calculate_cached(req_hash: str, req_json: str) -> CalculationResult:
    req = CalculationRequest.model_validate_json(req_json)
    return calculate(req)
```

### 7. 📉 Отсутствие мониторинга и метрик
**Приоритет: СРЕДНИЙ**

**Проблема:**
Нет отслеживания:
- Количества запросов к каждому эндпоинту
- Времени выполнения расчётов
- Частоты ошибок валидации
- Использования live CBR vs static rates

**Решение:**
Интегрировать Prometheus + Grafana:
```python
from prometheus_fastapi_instrumentator import Instrumentator

app = create_app()
Instrumentator().instrument(app).expose(app)
```

### 8. 🌐 Отсутствие интернационализации (i18n)
**Приоритет: СРЕДНИЙ**

**Проблема:**
Все сообщения только на русском языке, что ограничивает использование для международных клиентов.

**Решение:**
```python
# app/core/messages.py
from enum import Enum

class Locale(str, Enum):
    RU = "ru"
    EN = "en"
    
MESSAGES = {
    "ERR_YEAR_FUTURE": {
        Locale.RU: "Год выпуска не может быть в будущем",
        Locale.EN: "Year cannot be in the future"
    }
}

def get_message(key: str, locale: Locale = Locale.RU) -> str:
    return MESSAGES[key][locale]
```

### 9. 🔍 Недостаточное логирование бизнес-событий
**Приоритет: СРЕДНИЙ**

**Проблема:**
Логируется только успешный расчёт, но нет логов:
- Отклонённых запросов (валидация)
- Использования fallback-значений (дефолтные фрахты)
- Времени выполнения компонентов

**Решение:**
```python
@logger.catch  # Автоматическое логирование исключений
def calculate(req: CalculationRequest) -> CalculationResult:
    start_time = time.time()
    try:
        # ... расчёт ...
    finally:
        duration_ms = (time.time() - start_time) * 1000
        logger.info("calculation_timing", duration_ms=duration_ms)
```

### 10. 🧪 Отсутствие нагрузочного тестирования
**Приоритет: СРЕДНИЙ**

**Проблема:**
Неизвестна пропускная способность системы под нагрузкой.

**Решение:**
Создать `tests/load/locustfile.py`:
```python
from locust import HttpUser, task, between

class CalculatorUser(HttpUser):
    wait_time = between(1, 3)
    
    @task
    def calculate(self):
        self.client.post("/api/calculate", json={
            "country": "japan",
            "year": 2021,
            "engine_cc": 1500,
            "purchase_price": 1000000,
            "currency": "JPY"
        })
```

---

## Низкоприоритетные улучшения

### 11. 📦 Дублирование зависимостей в pyproject.toml
**Приоритет: НИЗКИЙ**

**Проблема:**
Зависимости перечислены дважды: в `[project]` и `[tool.poetry.dependencies]`.

**Решение:**
Оставить только Poetry-секцию:
```toml
[tool.poetry.dependencies]
python = ">=3.13,<4.0"
# ...
```

### 12. 🎨 Отсутствие CI/CD пайплайна
**Приоритет: НИЗКИЙ**

**Проблема:**
Нет автоматических проверок при коммитах (lint, tests, build).

**Решение:**
Создать `.github/workflows/ci.yml`:
```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install poetry
      - run: poetry install
      - run: poetry run pytest
      - run: poetry run ruff check
      - run: poetry run mypy app
```

### 13. 🗂 Отсутствие миграций для конфигураций
**Приоритет: НИЗКИЙ**

**Проблема:**
При изменении структуры YAML-конфигов нет версионирования и валидации схемы.

**Решение:**
Добавить Pydantic-модели для конфигов:
```python
class FeesConfig(BaseModel):
    country_currency: str
    tiers: list[dict]
    freight: dict
    
def _read_yaml_validated(name: str, model: type[BaseModel]):
    data = _read_yaml(name)
    return model.model_validate(data)
```

### 14. 🔧 Отсутствие graceful shutdown для бота
**Приоритет: НИЗКИЙ**

**Проблема:**
При остановке контейнера бот может потерять незавершённые запросы.

**Решение:**
```python
import signal

async def shutdown(dispatcher: Dispatcher):
    logger.info("Graceful shutdown initiated")
    await dispatcher.stop_polling()

signal.signal(signal.SIGTERM, lambda s, f: asyncio.create_task(shutdown(dp)))
```

### 15. 📱 PWA не поддерживает офлайн-расчёты
**Приоритет: НИЗКИЙ**

**Проблема:**
Service Worker кэширует только статику, но не данные тарифов.

**Решение:**
В `sw.js`:
```javascript
// Кэшировать /api/rates и /api/meta при установке SW
self.addEventListener('install', (event) => {
    event.waitUntil(
        caches.open('api-cache-v1').then((cache) => {
            return cache.addAll([
                '/api/rates',
                '/api/meta'
            ]);
        })
    );
});
```

---

## Архитектурные улучшения

### 16. 🏗 Отсутствие слоя абстракции для конфигураций
**Приоритет: СРЕДНИЙ**

**Проблема:**
Прямое обращение к словарям конфигов затрудняет:
- Типизацию
- Валидацию схемы
- Рефакторинг

**Решение:**
Создать `app/core/config_models.py`:
```python
from pydantic import BaseModel

class CountryFees(BaseModel):
    country_currency: str
    tiers: list[dict] | None = None
    base_expenses: dict[str, int] | None = None
    freight: dict[str, dict]
    
class ConfigRegistry(BaseModel):
    fees: dict[str, CountryFees]
    # ...
```

### 17. 🔗 Тесная связанность engine.py с services.cbr
**Приоритет: СРЕДНИЙ**

**Проблема:**
`engine.py` напрямую импортирует `get_effective_rates()`, нарушая принцип инверсии зависимостей.

**Решение:**
Использовать dependency injection:
```python
def calculate(
    req: CalculationRequest,
    rate_provider: Callable[[dict], dict] = get_effective_rates
) -> CalculationResult:
    rates_conf = rate_provider(configs.rates)
    # ...
```

### 18. 🧩 Отсутствие бизнес-слоя (domain layer)
**Приоритет: НИЗКИЙ**

**Проблема:**
Бизнес-логика смешана с инфраструктурным кодом.

**Решение:**
Создать `app/domain/`:
```
app/domain/
  entities.py      # Vehicle, Calculation
  repositories.py  # ConfigRepository (интерфейс)
  services.py      # CalculationService (оркестрация)
```

---

## Рекомендации по внедрению

### Фаза 1: Критические исправления (1 неделя)
1. Исправить падающий тест
2. Добавить юнит-тесты для округления и валидации
3. Добавить авторизацию для `/api/rates/refresh`

### Фаза 2: Улучшение надёжности (2 недели)
4. Внедрить Redis-based rate limiting
5. Добавить мониторинг (Prometheus)
6. Настроить CI/CD пайплайн
7. Добавить API документацию

### Фаза 3: Оптимизация (2 недели)
8. Внедрить кэширование результатов
9. Добавить нагрузочное тестирование
10. Улучшить логирование бизнес-событий

### Фаза 4: Архитектурные улучшения (опционально)
11. Refactoring к чистой архитектуре
12. Добавление i18n
13. Офлайн-расчёты в PWA

---

## Метрики успеха

После внедрения улучшений ожидаются:
- ✅ 100% прохождение тестов
- ✅ Покрытие тестами >85%
- ✅ Время ответа API <100ms (p95)
- ✅ Поддержка >1000 req/min на 1 инстансе
- ✅ Zero downtime deployments
- ✅ Автоматическое обнаружение регрессий через CI

---

## Заключение

Проект имеет **солидный фундамент**, но нуждается в:
1. **Срочном исправлении падающего теста**
2. **Усилении тестового покрытия** (особенно юнит-тесты)
3. **Улучшении безопасности** (rate limiting, авторизация)
4. **Внедрении мониторинга** для production-окружения

Рекомендуется начать с **Фазы 1** и последовательно двигаться к более сложным архитектурным улучшениям.

